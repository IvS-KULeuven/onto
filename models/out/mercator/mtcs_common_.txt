mtcs_common (Library):
    enums (Namespace):
        InitializationStates (Enum):
            SHUTDOWN (EnumItem):
            INITIALIZING (EnumItem):
            INITIALIZING_FAILED (EnumItem):
            INITIALIZED (EnumItem):
            LOCKED (EnumItem):
        OperatorStates (Enum):
            NONE (EnumItem):
            OBSERVER (EnumItem):
            TECH (EnumItem):
        OperatingStates (Enum):
            AUTO (EnumItem):
            MANUAL (EnumItem):
        OperatingModeStates (Enum):
            LOCAL (EnumItem):
            REMOTE (EnumItem):
        LimitSwitches (Enum):
            POSITIVE_LIMIT_SWITCH (EnumItem):
            NEGATIVE_LIMIT_SWITCH (EnumItem):
        Units (Enum):
            DEGREE (EnumItem):
            RADIAN (EnumItem):
            PERCENT (EnumItem):
            RADIANS_PER_SECOND (EnumItem):
            DEGREES_PER_SECOND (EnumItem):
            MILLIMETERS_PER_SECOND (EnumItem):
            REVOLUTIONS_PER_SECOND (EnumItem):
            REVOLUTIONS_PER_MINUTE (EnumItem):
            AMPS (EnumItem):
            MILLIAMPS (EnumItem):
            MILLIMETER (EnumItem):
            MICROMETER (EnumItem):
            DEGREES_CELSIUS (EnumItem):
            KELVIN (EnumItem):
            BAR (EnumItem):
            PASCAL (EnumItem):
            UNITLESS (EnumItem):
            NEWTON (EnumItem):
            DECANEWTON (EnumItem):
            VOLT (EnumItem):
            MILLIVOLT (EnumItem):
            HERTZ (EnumItem):
            G (EnumItem):
            MILLIG (EnumItem):
            NEWTONMETER (EnumItem):
            ARCSECONDS (EnumItem):
            ARCSECONDS_PER_SECOND (EnumItem):
            RADIANS_PER_SQUARE_SECOND (EnumItem):
            DEGREES_PER_SQUARE_SECOND (EnumItem):
            ARCSECONDS_PER_SQUARE_SECOND (EnumItem):
            METERS_PER_SECOND (EnumItem):
            HECTOPASCAL (EnumItem):
            SECONDS (EnumItem):
            MINUTES (EnumItem):
            MILLIMETERS_PER_HOUR (EnumItem):
            HITS_PER_SQUARE_CENTIMETER (EnumItem):
            HITS_PER_SQUARE_CENTIMETER_PER_HOUR (EnumItem):
        RequestResults (Enum):
            ACCEPTED (EnumItem):
            REJECTED (EnumItem):
        DriveOperatingModes (Enum):
            OPERATING_MODE_NONE (EnumItem):
            OPERATING_MODE_TORQUE_CONTROL (EnumItem):
            OPERATING_MODE_VELOCITY_CONTROL (EnumItem):
            OPERATING_MODE_POSITION_CONTROL (EnumItem):
        DriveBrakeStates (Enum):
            BRAKE_AUTOMATIC (EnumItem):
            BRAKE_FORCE_LOCK (EnumItem):
            BRAKE_FORCE_UNLOCK (EnumItem):
    statuses (Namespace):
        ApertureStatus (Status):
            superState (Variable):
            isOpen (Variable):
            isClosed (Variable):
            open (Variable):
            closed (Variable):
            partiallyOpen (Variable):
            undefined (Variable):
        BusyStatus (Status):
            superState (Variable):
            isBusy (Variable):
            idle (Variable):
            busy (Variable):
        StartedStatus (Status):
            superState (Variable):
            isStarted (Variable):
            started (Variable):
            notStarted (Variable):
        EnabledStatus (Status):
            superState (Variable):
            isEnabled (Variable):
            enabled (Variable):
            disabled (Variable):
        PoweredStatus (Status):
            superState (Variable):
            isEnabled (Variable):
            enabled (Variable):
            disabled (Variable):
        HealthStatus (Status):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
        InitializationStatus (Status):
            superState (Variable):
            state (Variable):
            shutdown (Variable):
            initializing (Variable):
            initializingFailed (Variable):
            initialized (Variable):
            locked (Variable):
        MotionStatus (Status):
            superState (Variable):
            actVel (Variable):
            tolerance (Variable):
            forward (Variable):
            backward (Variable):
            standstill (Variable):
        HiHiLoLoAlarmStatus (Status):
            superState (Variable):
            config (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
            value (Variable):
            disabled (Variable):
            hiHi (Variable):
            hi (Variable):
            ok (Variable):
            lo (Variable):
            loLo (Variable):
        OpeningStatus (Status):
            superState (Variable):
            isOpening (Variable):
            isClosing (Variable):
            opening (Variable):
            closing (Variable):
            standstill (Variable):
        OperatorStatus (Status):
            superState (Variable):
            state (Variable):
            none (Variable):
            tech (Variable):
            observer (Variable):
        OperatingStatus (Status):
            superState (Variable):
            state (Variable):
            auto (Variable):
            manual (Variable):
        OperatingModeStatus (Status):
            superState (Variable):
            state (Variable):
            local (Variable):
            remote (Variable):
        ActivityStatus (Status):
            superState (Variable):
            isMoving (Variable):
            isAwake (Variable):
            moving (Variable):
            awake (Variable):
            sleeping (Variable):
    statemachines (StatemachinesNamespace):
        parts (Namespace):
            AX52XXDriveParts (Struct):
                channelA (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    errorC1D (Variable):
                    isEnabled (Variable):
                    torqueFeedbackValue (Variable):
                    torqueCommandValue (Variable):
                    digitalInputs (Variable):
                    digitalOutputs (Variable):
                    safetyOptionState (Variable):
                    diagnosticNumber (Variable):
                    isInSafetate (Variable):
                    actualTorqueFeedback (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newNewtonmeterValue (Variable):
                    channelPeakCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    channelRatedCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    continuousStallTorque (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newNewtonmeterValue (Variable):
                    continuousStallCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    torqueConstant (Variable):
                    bipolarTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    positiveTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    negativeTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    operatingMode (Variable):
                    operatingModeDescription (Variable):
                    brakeState (Variable):
                    brakeStateDescription (Variable):
                    torqueCommand (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    safetyErrorAck (Variable):
                    statuses (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                    processes (Variable):
                        read (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                            set (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                            get (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                        write (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                            set (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                                value1Byte (Variable):
                                value2Bytes (Variable):
                                value4Bytes (Variable):
                            get (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                                value1Byte (Variable):
                                value2Bytes (Variable):
                                value4Bytes (Variable):
                        update (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            testVar (Variable):
                        reset (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                        acknowledgeSafetyError (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            testVar (Variable):
                        setBrake (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            set (Variable):
                                newState (Variable):
                            get (Variable):
                                newState (Variable):
                channelB (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    errorC1D (Variable):
                    isEnabled (Variable):
                    torqueFeedbackValue (Variable):
                    torqueCommandValue (Variable):
                    digitalInputs (Variable):
                    digitalOutputs (Variable):
                    safetyOptionState (Variable):
                    diagnosticNumber (Variable):
                    isInSafetate (Variable):
                    actualTorqueFeedback (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newNewtonmeterValue (Variable):
                    channelPeakCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    channelRatedCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    continuousStallTorque (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newNewtonmeterValue (Variable):
                    continuousStallCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    torqueConstant (Variable):
                    bipolarTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    positiveTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    negativeTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    operatingMode (Variable):
                    operatingModeDescription (Variable):
                    brakeState (Variable):
                    brakeStateDescription (Variable):
                    torqueCommand (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    safetyErrorAck (Variable):
                    statuses (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                    processes (Variable):
                        read (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                            set (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                            get (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                        write (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                            set (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                                value1Byte (Variable):
                                value2Bytes (Variable):
                                value4Bytes (Variable):
                            get (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                                value1Byte (Variable):
                                value2Bytes (Variable):
                                value4Bytes (Variable):
                        update (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            testVar (Variable):
                        reset (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                        acknowledgeSafetyError (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            testVar (Variable):
                        setBrake (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            set (Variable):
                                newState (Variable):
                            get (Variable):
                                newState (Variable):
        processes (Namespace):
            BaseAxisProcesses (Struct):
                moveAbsolute (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        position (Variable):
                        maxVelocity (Variable):
                    get (Variable):
                        position (Variable):
                        maxVelocity (Variable):
                moveRelative (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        distance (Variable):
                        maxVelocity (Variable):
                    get (Variable):
                        distance (Variable):
                        maxVelocity (Variable):
                moveVelocity (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        value (Variable):
                    get (Variable):
                        value (Variable):
                reset (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                stop (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                stopParametrized (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        deceleration (Variable):
                        jerk (Variable):
                    get (Variable):
                        deceleration (Variable):
                        jerk (Variable):
                power (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        enable (Variable):
                    get (Variable):
                        enable (Variable):
                gearIn (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                gearOut (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                initialize (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                setPosition (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        value (Variable):
                    get (Variable):
                        value (Variable):
                enableExtSetGen (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                disableExtSetGen (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                forceCalibration (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                resetCalibration (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                readParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    value (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                readBoolParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    value (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                writeParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                writeBoolParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                probeStart (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    recordedPosition (Variable):
                    set (Variable):
                        risingEdge (Variable):
                        timeout (Variable):
                    get (Variable):
                        risingEdge (Variable):
                        timeout (Variable):
                probeAbort (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
            FaulhaberDriveProcesses (Struct):
                write (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        index (Variable):
                        subindex (Variable):
                        noOfBytes (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                    get (Variable):
                        index (Variable):
                        subindex (Variable):
                        noOfBytes (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                read (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value1Byte (Variable):
                    value2Bytes (Variable):
                    value4Bytes (Variable):
                    errorId (Variable):
                    set (Variable):
                        index (Variable):
                        subindex (Variable):
                        noOfBytes (Variable):
                    get (Variable):
                        index (Variable):
                        subindex (Variable):
                        noOfBytes (Variable):
                initialize (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
            AX52XXDriveChannelProcesses (Struct):
                read (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value1Byte (Variable):
                    value2Bytes (Variable):
                    value4Bytes (Variable):
                    adsErrorId (Variable):
                    sercosErrorId (Variable):
                    set (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                    get (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                write (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    adsErrorId (Variable):
                    sercosErrorId (Variable):
                    set (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                    get (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                update (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                reset (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    adsErrorId (Variable):
                    sercosErrorId (Variable):
                acknowledgeSafetyError (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                setBrake (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    set (Variable):
                        newState (Variable):
                    get (Variable):
                        newState (Variable):
            IncrementalEncoderProcesses (Struct):
                enableCounterResetC (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                disableCounterResetC (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
            SimpleRelayProcesses (Struct):
                setEnabled (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    set (Variable):
                        enabled (Variable):
                    get (Variable):
                        enabled (Variable):
            ConfigManagerProcesses (Struct):
                save (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        filePath (Variable):
                    get (Variable):
                        filePath (Variable):
                load (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        filePath (Variable):
                    get (Variable):
                        filePath (Variable):
                activate (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
            ModbusRTUBusProcesses (Struct):
                readCoil (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value (Variable):
                    errorId (Variable):
                    set (Variable):
                        unitID (Variable):
                        address (Variable):
                    get (Variable):
                        unitID (Variable):
                        address (Variable):
                writeCoil (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        unitID (Variable):
                        address (Variable):
                        value (Variable):
                    get (Variable):
                        unitID (Variable):
                        address (Variable):
                        value (Variable):
                readRegister (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value (Variable):
                    errorId (Variable):
                    set (Variable):
                        unitID (Variable):
                        address (Variable):
                    get (Variable):
                        unitID (Variable):
                        address (Variable):
                writeRegister (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        unitID (Variable):
                        address (Variable):
                        value (Variable):
                    get (Variable):
                        unitID (Variable):
                        address (Variable):
                        value (Variable):
                diagnostics (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value (Variable):
                    errorId (Variable):
                    set (Variable):
                        unitID (Variable):
                        subFunction (Variable):
                        subData (Variable):
                    get (Variable):
                        unitID (Variable):
                        subFunction (Variable):
                        subData (Variable):
        statuses (Namespace):
            BaseProcessStatuses (Struct):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            ProcessStepStatuses (Struct):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
            BaseAxisStatuses (Struct):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                poweredStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                extSetGenStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                motionStatus (Variable):
                    superState (Variable):
                    actVel (Variable):
                    tolerance (Variable):
                    forward (Variable):
                    backward (Variable):
                    standstill (Variable):
            FaulhaberDriveStatuses (Struct):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            AX52XXDriveChannelStatuses (Struct):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            AX52XXDriveStatuses (Struct):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            SSIEncoderStatuses (Struct):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            IncrementalEncoderStatuses (Struct):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
            EtherCatDeviceStatuses (Struct):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            CANopenBusStatuses (Struct):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            CurrentMeasurementStatuses (Struct):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            VoltageMeasurementStatuses (Struct):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            TemperatureMeasurementStatuses (Struct):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            PressureMeasurementStatuses (Struct):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            FrequencyMeasurementStatuses (Struct):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            GForceMeasurementStatuses (Struct):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            RelativeHumidityMeasurementStatuses (Struct):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            ForceMeasurementStatuses (Struct):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            LinearPositionMeasurement16Statuses (Struct):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            LinearPositionMeasurementU32Statuses (Struct):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            SimpleRelayStatuses (Struct):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
            ConfigManagerStatuses (Struct):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            ModbusRTUBusStatuses (Struct):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
        SM_QuantityValue (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_AngularPosition (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            radians (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            degrees (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            arcseconds (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newDegreesValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_LinearPosition (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            millimeters (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            micrometers (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMillimetersValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_AngularVelocity (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            radiansPerSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            degreesPerSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            arcsecondsPerSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newDegreesPerSecondValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_AngularAcceleration (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            radiansPerSquareSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            degreesPerSquareSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            arcsecondsPerSquareSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newDegreesPerSquareSecondValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_LinearVelocity (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            millimetersPerSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMillimetersPerSecondValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Voltage (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            volt (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliVolt (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newVoltValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_GForce (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            g (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliG (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMilliGValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Current (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            amps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliAmps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newAmpsValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Torque (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            newtonmeter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newNewtonmeterValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_TorqueLimit (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            unitless (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            percent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newtonmeter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newFractionValue (Variable):
            maxNewtonmeter (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_AverageCurrent (Statemachine):
            SUPER (Pointer):
               points_to_type: Current
            actualStatus (Variable):
            previousStatus (Variable):
            amps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliAmps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newAmpsValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Duration (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            seconds (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            minutes (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newSecondsValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Temperature (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            celsius (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            kelvin (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newCelsiusValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Pressure (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            bar (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            pascal (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            hectoPascal (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newBarValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Frequency (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            hertz (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newHertzValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Force (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            newton (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            decaNewton (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newNewtonValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Fraction (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            unitless (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            percent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newFractionValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_BaseProcess (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_BaseMcProcess (Statemachine):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
        SM_ProcessStep (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            process (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_BaseXmlDataSrvProcess (Statemachine):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
        SM_BaseAxis (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            axis_ref (Variable):
                PlcToNc (Variable):
                    ControlDWord (Variable):
                    Override (Variable):
                    AxisModeRequest (Variable):
                    AxisModeDWord (Variable):
                    AxisModeLReal (Variable):
                    PositionCorrection (Variable):
                    ExtSetPos (Variable):
                    ExtSetVelo (Variable):
                    ExtSetAcc (Variable):
                    ExtSetDirection (Variable):
                    ExtControllerOutput (Variable):
                    GearRatio1 (Variable):
                    GearRatio2 (Variable):
                    GearRatio3 (Variable):
                    GearRatio4 (Variable):
                NcToPlc (Variable):
                    StateDWord (Variable):
                    ErrorCode (Variable):
                    AxisState (Variable):
                    AxisModeConfirmation (Variable):
                    HomingState (Variable):
                    CoupleState (Variable):
                    SvbEntries (Variable):
                    SafEntries (Variable):
                    AxisId (Variable):
                    OpModeDWord (Variable):
                    ActiveControlLoopIndex (Variable):
                    ControlLoopIndex (Variable):
                    ActPos (Variable):
                    ModuloActPos (Variable):
                    ModuloActTurns (Variable):
                    ActVelo (Variable):
                    PosDiff (Variable):
                    SetPos (Variable):
                    SetVelo (Variable):
                    SetAcc (Variable):
                    TargetPos (Variable):
                    ModuloSetPos (Variable):
                    ModuloSetTurns (Variable):
                    CmdNo (Variable):
                    CmdState (Variable):
                Status (Variable):
                    UpdateTaskIndex (Variable):
                    UpdateCycleTime (Variable):
                    CycleCounter (Variable):
                    NcCycleCounter (Variable):
                    Error (Variable):
                    ErrorId (Variable):
                    Disabled (Variable):
                    ControlLoopClosed (Variable):
                    ExtSetPointGenEnabled (Variable):
            isGearingSupported (Variable):
            isJogEnabled (Variable):
            isEnabled (Variable):
            standstillTolerance (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                poweredStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                extSetGenStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                motionStatus (Variable):
                    superState (Variable):
                    actVel (Variable):
                    tolerance (Variable):
                    forward (Variable):
                    backward (Variable):
                    standstill (Variable):
            processes (Variable):
                moveAbsolute (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        position (Variable):
                        maxVelocity (Variable):
                    get (Variable):
                        position (Variable):
                        maxVelocity (Variable):
                moveRelative (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        distance (Variable):
                        maxVelocity (Variable):
                    get (Variable):
                        distance (Variable):
                        maxVelocity (Variable):
                moveVelocity (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        value (Variable):
                    get (Variable):
                        value (Variable):
                reset (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                stop (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                stopParametrized (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        deceleration (Variable):
                        jerk (Variable):
                    get (Variable):
                        deceleration (Variable):
                        jerk (Variable):
                power (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        enable (Variable):
                    get (Variable):
                        enable (Variable):
                gearIn (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                gearOut (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                initialize (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                setPosition (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        value (Variable):
                    get (Variable):
                        value (Variable):
                enableExtSetGen (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                disableExtSetGen (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                forceCalibration (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                resetCalibration (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                readParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    value (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                readBoolParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    value (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                writeParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                writeBoolParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                probeStart (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    recordedPosition (Variable):
                    set (Variable):
                        risingEdge (Variable):
                        timeout (Variable):
                    get (Variable):
                        risingEdge (Variable):
                        timeout (Variable):
                probeAbort (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
            moveAbsolute (Method):
                position (Variable):
                maxVelocity (Variable):
            moveRelative (Method):
                distance (Variable):
                maxVelocity (Variable):
            moveVelocity (Method):
                value (Variable):
            reset (Method):
            stop (Method):
            stopParametrized (Method):
                deceleration (Variable):
                jerk (Variable):
            power (Method):
                enable (Variable):
            gearIn (Method):
            gearOut (Method):
            initialize (Method):
            setPosition (Method):
                value (Variable):
            enableExtSetGen (Method):
            disableExtSetGen (Method):
            forceCalibration (Method):
            resetCalibration (Method):
            readParameter (Method):
                parameterNumber (Variable):
            readBoolParameter (Method):
                parameterNumber (Variable):
            writeParameter (Method):
                parameterNumber (Variable):
                value (Variable):
            writeBoolParameter (Method):
                parameterNumber (Variable):
                value (Variable):
            probeStart (Method):
                risingEdge (Variable):
                timeout (Variable):
            probeAbort (Method):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_AngularAxis (Statemachine):
            SUPER (Pointer):
               points_to_type: BaseAxis
            actualStatus (Variable):
            previousStatus (Variable):
            axis_ref (Variable):
                PlcToNc (Variable):
                    ControlDWord (Variable):
                    Override (Variable):
                    AxisModeRequest (Variable):
                    AxisModeDWord (Variable):
                    AxisModeLReal (Variable):
                    PositionCorrection (Variable):
                    ExtSetPos (Variable):
                    ExtSetVelo (Variable):
                    ExtSetAcc (Variable):
                    ExtSetDirection (Variable):
                    ExtControllerOutput (Variable):
                    GearRatio1 (Variable):
                    GearRatio2 (Variable):
                    GearRatio3 (Variable):
                    GearRatio4 (Variable):
                NcToPlc (Variable):
                    StateDWord (Variable):
                    ErrorCode (Variable):
                    AxisState (Variable):
                    AxisModeConfirmation (Variable):
                    HomingState (Variable):
                    CoupleState (Variable):
                    SvbEntries (Variable):
                    SafEntries (Variable):
                    AxisId (Variable):
                    OpModeDWord (Variable):
                    ActiveControlLoopIndex (Variable):
                    ControlLoopIndex (Variable):
                    ActPos (Variable):
                    ModuloActPos (Variable):
                    ModuloActTurns (Variable):
                    ActVelo (Variable):
                    PosDiff (Variable):
                    SetPos (Variable):
                    SetVelo (Variable):
                    SetAcc (Variable):
                    TargetPos (Variable):
                    ModuloSetPos (Variable):
                    ModuloSetTurns (Variable):
                    CmdNo (Variable):
                    CmdState (Variable):
                Status (Variable):
                    UpdateTaskIndex (Variable):
                    UpdateCycleTime (Variable):
                    CycleCounter (Variable):
                    NcCycleCounter (Variable):
                    Error (Variable):
                    ErrorId (Variable):
                    Disabled (Variable):
                    ControlLoopClosed (Variable):
                    ExtSetPointGenEnabled (Variable):
            isGearingSupported (Variable):
            isJogEnabled (Variable):
            isEnabled (Variable):
            standstillTolerance (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                poweredStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                extSetGenStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                motionStatus (Variable):
                    superState (Variable):
                    actVel (Variable):
                    tolerance (Variable):
                    forward (Variable):
                    backward (Variable):
                    standstill (Variable):
            processes (Variable):
                moveAbsolute (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        position (Variable):
                        maxVelocity (Variable):
                    get (Variable):
                        position (Variable):
                        maxVelocity (Variable):
                moveRelative (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        distance (Variable):
                        maxVelocity (Variable):
                    get (Variable):
                        distance (Variable):
                        maxVelocity (Variable):
                moveVelocity (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        value (Variable):
                    get (Variable):
                        value (Variable):
                reset (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                stop (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                stopParametrized (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        deceleration (Variable):
                        jerk (Variable):
                    get (Variable):
                        deceleration (Variable):
                        jerk (Variable):
                power (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        enable (Variable):
                    get (Variable):
                        enable (Variable):
                gearIn (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                gearOut (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                initialize (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                setPosition (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        value (Variable):
                    get (Variable):
                        value (Variable):
                enableExtSetGen (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                disableExtSetGen (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                forceCalibration (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                resetCalibration (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                readParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    value (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                readBoolParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    value (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                writeParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                writeBoolParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                probeStart (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    recordedPosition (Variable):
                    set (Variable):
                        risingEdge (Variable):
                        timeout (Variable):
                    get (Variable):
                        risingEdge (Variable):
                        timeout (Variable):
                probeAbort (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
            moveAbsolute (Method):
                position (Variable):
                maxVelocity (Variable):
            moveRelative (Method):
                distance (Variable):
                maxVelocity (Variable):
            moveVelocity (Method):
                value (Variable):
            reset (Method):
            stop (Method):
            stopParametrized (Method):
                deceleration (Variable):
                jerk (Variable):
            power (Method):
                enable (Variable):
            gearIn (Method):
            gearOut (Method):
            initialize (Method):
            setPosition (Method):
                value (Variable):
            enableExtSetGen (Method):
            disableExtSetGen (Method):
            forceCalibration (Method):
            resetCalibration (Method):
            readParameter (Method):
                parameterNumber (Variable):
            readBoolParameter (Method):
                parameterNumber (Variable):
            writeParameter (Method):
                parameterNumber (Variable):
                value (Variable):
            writeBoolParameter (Method):
                parameterNumber (Variable):
                value (Variable):
            probeStart (Method):
                risingEdge (Variable):
                timeout (Variable):
            probeAbort (Method):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            actPos (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                radians (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                degrees (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                arcseconds (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newDegreesValue (Variable):
            actVel (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                radiansPerSecond (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                degreesPerSecond (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                arcsecondsPerSecond (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newDegreesPerSecondValue (Variable):
        SM_LinearAxis (Statemachine):
            SUPER (Pointer):
               points_to_type: BaseAxis
            actualStatus (Variable):
            previousStatus (Variable):
            axis_ref (Variable):
                PlcToNc (Variable):
                    ControlDWord (Variable):
                    Override (Variable):
                    AxisModeRequest (Variable):
                    AxisModeDWord (Variable):
                    AxisModeLReal (Variable):
                    PositionCorrection (Variable):
                    ExtSetPos (Variable):
                    ExtSetVelo (Variable):
                    ExtSetAcc (Variable):
                    ExtSetDirection (Variable):
                    ExtControllerOutput (Variable):
                    GearRatio1 (Variable):
                    GearRatio2 (Variable):
                    GearRatio3 (Variable):
                    GearRatio4 (Variable):
                NcToPlc (Variable):
                    StateDWord (Variable):
                    ErrorCode (Variable):
                    AxisState (Variable):
                    AxisModeConfirmation (Variable):
                    HomingState (Variable):
                    CoupleState (Variable):
                    SvbEntries (Variable):
                    SafEntries (Variable):
                    AxisId (Variable):
                    OpModeDWord (Variable):
                    ActiveControlLoopIndex (Variable):
                    ControlLoopIndex (Variable):
                    ActPos (Variable):
                    ModuloActPos (Variable):
                    ModuloActTurns (Variable):
                    ActVelo (Variable):
                    PosDiff (Variable):
                    SetPos (Variable):
                    SetVelo (Variable):
                    SetAcc (Variable):
                    TargetPos (Variable):
                    ModuloSetPos (Variable):
                    ModuloSetTurns (Variable):
                    CmdNo (Variable):
                    CmdState (Variable):
                Status (Variable):
                    UpdateTaskIndex (Variable):
                    UpdateCycleTime (Variable):
                    CycleCounter (Variable):
                    NcCycleCounter (Variable):
                    Error (Variable):
                    ErrorId (Variable):
                    Disabled (Variable):
                    ControlLoopClosed (Variable):
                    ExtSetPointGenEnabled (Variable):
            isGearingSupported (Variable):
            isJogEnabled (Variable):
            isEnabled (Variable):
            standstillTolerance (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                poweredStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                extSetGenStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                motionStatus (Variable):
                    superState (Variable):
                    actVel (Variable):
                    tolerance (Variable):
                    forward (Variable):
                    backward (Variable):
                    standstill (Variable):
            processes (Variable):
                moveAbsolute (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        position (Variable):
                        maxVelocity (Variable):
                    get (Variable):
                        position (Variable):
                        maxVelocity (Variable):
                moveRelative (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        distance (Variable):
                        maxVelocity (Variable):
                    get (Variable):
                        distance (Variable):
                        maxVelocity (Variable):
                moveVelocity (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        value (Variable):
                    get (Variable):
                        value (Variable):
                reset (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                stop (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                stopParametrized (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        deceleration (Variable):
                        jerk (Variable):
                    get (Variable):
                        deceleration (Variable):
                        jerk (Variable):
                power (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        enable (Variable):
                    get (Variable):
                        enable (Variable):
                gearIn (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                gearOut (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                initialize (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                setPosition (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        value (Variable):
                    get (Variable):
                        value (Variable):
                enableExtSetGen (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                disableExtSetGen (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                forceCalibration (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                resetCalibration (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                readParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    value (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                readBoolParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    value (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                writeParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                writeBoolParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                probeStart (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    recordedPosition (Variable):
                    set (Variable):
                        risingEdge (Variable):
                        timeout (Variable):
                    get (Variable):
                        risingEdge (Variable):
                        timeout (Variable):
                probeAbort (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
            moveAbsolute (Method):
                position (Variable):
                maxVelocity (Variable):
            moveRelative (Method):
                distance (Variable):
                maxVelocity (Variable):
            moveVelocity (Method):
                value (Variable):
            reset (Method):
            stop (Method):
            stopParametrized (Method):
                deceleration (Variable):
                jerk (Variable):
            power (Method):
                enable (Variable):
            gearIn (Method):
            gearOut (Method):
            initialize (Method):
            setPosition (Method):
                value (Variable):
            enableExtSetGen (Method):
            disableExtSetGen (Method):
            forceCalibration (Method):
            resetCalibration (Method):
            readParameter (Method):
                parameterNumber (Variable):
            readBoolParameter (Method):
                parameterNumber (Variable):
            writeParameter (Method):
                parameterNumber (Variable):
                value (Variable):
            writeBoolParameter (Method):
                parameterNumber (Variable):
                value (Variable):
            probeStart (Method):
                risingEdge (Variable):
                timeout (Variable):
            probeAbort (Method):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            actPos (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                millimeters (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                micrometers (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newMillimetersValue (Variable):
            actVel (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                millimetersPerSecond (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newMillimetersPerSecondValue (Variable):
        SM_FaulhaberDrive (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            config (Variable):
                sendAtInitialization (Variable):
                CANopenBusNetId (Variable):
                CANopenNodePort (Variable):
                modesOfOperation (Variable):
                polarity (Variable):
                maxProfileVelocity (Variable):
                negativeLimitSwitch (Variable):
                positiveLimitSwitch (Variable):
                homingSwitch (Variable):
                limitSwitchPolarity (Variable):
                continuousCurrentLimit (Variable):
                peakCurrentLimit (Variable):
                velocityControlProportionalTerm (Variable):
                velocityControlIntegralTerm (Variable):
            actualCurrent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            averageCurrent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            isEnabled (Variable):
            newMilliampsValue (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            processes (Variable):
                write (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        index (Variable):
                        subindex (Variable):
                        noOfBytes (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                    get (Variable):
                        index (Variable):
                        subindex (Variable):
                        noOfBytes (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                read (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value1Byte (Variable):
                    value2Bytes (Variable):
                    value4Bytes (Variable):
                    errorId (Variable):
                    set (Variable):
                        index (Variable):
                        subindex (Variable):
                        noOfBytes (Variable):
                    get (Variable):
                        index (Variable):
                        subindex (Variable):
                        noOfBytes (Variable):
                initialize (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
            write (Method):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            read (Method):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
            initialize (Method):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_AX52XXDriveChannel (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            errorC1D (Variable):
            isEnabled (Variable):
            torqueFeedbackValue (Variable):
            torqueCommandValue (Variable):
            digitalInputs (Variable):
            digitalOutputs (Variable):
            safetyOptionState (Variable):
            diagnosticNumber (Variable):
            isInSafetate (Variable):
            actualTorqueFeedback (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newNewtonmeterValue (Variable):
            channelPeakCurrent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            channelRatedCurrent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            continuousStallTorque (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newNewtonmeterValue (Variable):
            continuousStallCurrent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            torqueConstant (Variable):
            bipolarTorqueLimit (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
                maxNewtonmeter (Variable):
            positiveTorqueLimit (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
                maxNewtonmeter (Variable):
            negativeTorqueLimit (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
                maxNewtonmeter (Variable):
            operatingMode (Variable):
            operatingModeDescription (Variable):
            brakeState (Variable):
            brakeStateDescription (Variable):
            torqueCommand (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
                maxNewtonmeter (Variable):
            safetyErrorAck (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            processes (Variable):
                read (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value1Byte (Variable):
                    value2Bytes (Variable):
                    value4Bytes (Variable):
                    adsErrorId (Variable):
                    sercosErrorId (Variable):
                    set (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                    get (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                write (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    adsErrorId (Variable):
                    sercosErrorId (Variable):
                    set (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                    get (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                update (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                reset (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    adsErrorId (Variable):
                    sercosErrorId (Variable):
                acknowledgeSafetyError (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                setBrake (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    set (Variable):
                        newState (Variable):
                    get (Variable):
                        newState (Variable):
            read (Method):
                index (Variable):
                noOfBytes (Variable):
            write (Method):
                index (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            update (Method):
            reset (Method):
            acknowledgeSafetyError (Method):
            setBrake (Method):
                newState (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_AX52XXDrive (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            parts (Variable):
                channelA (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    errorC1D (Variable):
                    isEnabled (Variable):
                    torqueFeedbackValue (Variable):
                    torqueCommandValue (Variable):
                    digitalInputs (Variable):
                    digitalOutputs (Variable):
                    safetyOptionState (Variable):
                    diagnosticNumber (Variable):
                    isInSafetate (Variable):
                    actualTorqueFeedback (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newNewtonmeterValue (Variable):
                    channelPeakCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    channelRatedCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    continuousStallTorque (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newNewtonmeterValue (Variable):
                    continuousStallCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    torqueConstant (Variable):
                    bipolarTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    positiveTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    negativeTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    operatingMode (Variable):
                    operatingModeDescription (Variable):
                    brakeState (Variable):
                    brakeStateDescription (Variable):
                    torqueCommand (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    safetyErrorAck (Variable):
                    statuses (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                    processes (Variable):
                        read (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                            set (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                            get (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                        write (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                            set (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                                value1Byte (Variable):
                                value2Bytes (Variable):
                                value4Bytes (Variable):
                            get (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                                value1Byte (Variable):
                                value2Bytes (Variable):
                                value4Bytes (Variable):
                        update (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            testVar (Variable):
                        reset (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                        acknowledgeSafetyError (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            testVar (Variable):
                        setBrake (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            set (Variable):
                                newState (Variable):
                            get (Variable):
                                newState (Variable):
                channelB (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    errorC1D (Variable):
                    isEnabled (Variable):
                    torqueFeedbackValue (Variable):
                    torqueCommandValue (Variable):
                    digitalInputs (Variable):
                    digitalOutputs (Variable):
                    safetyOptionState (Variable):
                    diagnosticNumber (Variable):
                    isInSafetate (Variable):
                    actualTorqueFeedback (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newNewtonmeterValue (Variable):
                    channelPeakCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    channelRatedCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    continuousStallTorque (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newNewtonmeterValue (Variable):
                    continuousStallCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    torqueConstant (Variable):
                    bipolarTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    positiveTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    negativeTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    operatingMode (Variable):
                    operatingModeDescription (Variable):
                    brakeState (Variable):
                    brakeStateDescription (Variable):
                    torqueCommand (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    safetyErrorAck (Variable):
                    statuses (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                    processes (Variable):
                        read (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                            set (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                            get (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                        write (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                            set (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                                value1Byte (Variable):
                                value2Bytes (Variable):
                                value4Bytes (Variable):
                            get (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                                value1Byte (Variable):
                                value2Bytes (Variable):
                                value4Bytes (Variable):
                        update (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            testVar (Variable):
                        reset (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                        acknowledgeSafetyError (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            testVar (Variable):
                        setBrake (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            set (Variable):
                                newState (Variable):
                            get (Variable):
                                newState (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_SSIEncoder (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            dataError (Variable):
            frameError (Variable):
            powerFailure (Variable):
            syncError (Variable):
            counterValue (Variable):
            status (Variable):
            statuses (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_IncrementalEncoder (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            latchCValid (Variable):
            latchExternValid (Variable):
            setCounterDone (Variable):
            counterUnderflow (Variable):
            counterOverflow (Variable):
            statusOfInputStatus (Variable):
            openCircuit (Variable):
            extrapolationStall (Variable):
            statusOfInputA (Variable):
            statusOfInputB (Variable):
            statusOfInputC (Variable):
            statusOfInputGate (Variable):
            statusOfExternLatch (Variable):
            syncError (Variable):
            isEnabled (Variable):
            counterValue (Variable):
            status (Variable):
            setCounterValue (Variable):
            setCounter (Variable):
            enableLatchC (Variable):
            statuses (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
            processes (Variable):
                enableCounterResetC (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                disableCounterResetC (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
            enableCounterResetC (Method):
            disableCounterResetC (Method):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_EtherCatDevice (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            id (Variable):
            typeId (Variable):
            wcState (Variable):
            infoData (Variable):
            statuses (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_CANopenBus (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            error (Variable):
            canState (Variable):
            rxErrorCounter (Variable):
            txErrorCounter (Variable):
            statuses (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_CurrentMeasurement (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            current (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            microAmpsValue (Variable):
            error (Variable):
            statuses (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_VoltageMeasurement (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                volt (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliVolt (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newVoltValue (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                volt (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliVolt (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newVoltValue (Variable):
            underrange (Variable):
            overrange (Variable):
            rawValue (Variable):
            error (Variable):
            conversionFactor (Variable):
            config (Variable):
                enabled (Variable):
                gain (Variable):
                offset (Variable):
                alarms (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_TemperatureMeasurement (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                celsius (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                kelvin (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newCelsiusValue (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                celsius (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                kelvin (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newCelsiusValue (Variable):
            underrange (Variable):
            overrange (Variable):
            rawValue (Variable):
            error (Variable):
            conversionFactor (Variable):
            config (Variable):
                enabled (Variable):
                gain (Variable):
                offset (Variable):
                alarms (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_PressureMeasurement (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                bar (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                pascal (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                hectoPascal (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newBarValue (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                bar (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                pascal (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                hectoPascal (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newBarValue (Variable):
            underrange (Variable):
            overrange (Variable):
            rawValue (Variable):
            error (Variable):
            conversionFactor (Variable):
            config (Variable):
                enabled (Variable):
                gain (Variable):
                offset (Variable):
                alarms (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_FrequencyMeasurement (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                hertz (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newHertzValue (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                hertz (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newHertzValue (Variable):
            underrange (Variable):
            overrange (Variable):
            rawValue (Variable):
            error (Variable):
            conversionFactor (Variable):
            config (Variable):
                enabled (Variable):
                gain (Variable):
                offset (Variable):
                alarms (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_GForceMeasurement (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                g (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliG (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newMilliGValue (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                g (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliG (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newMilliGValue (Variable):
            milliGValue (Variable):
            error (Variable):
            statuses (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_RelativeHumidityMeasurement (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
            underrange (Variable):
            overrange (Variable):
            rawValue (Variable):
            error (Variable):
            conversionFactor (Variable):
            config (Variable):
                enabled (Variable):
                gain (Variable):
                offset (Variable):
                alarms (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_ForceMeasurement (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                newton (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                decaNewton (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newNewtonValue (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                newton (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                decaNewton (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newNewtonValue (Variable):
            rawValue (Variable):
            error (Variable):
            underrange (Variable):
            overrange (Variable):
            conversionFactor (Variable):
            config (Variable):
                enabled (Variable):
                gain (Variable):
                offset (Variable):
                alarms (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_LinearPositionMeasurement16 (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                millimeters (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                micrometers (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newMillimetersValue (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                millimeters (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                micrometers (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newMillimetersValue (Variable):
            rawValue (Variable):
            error (Variable):
            underrange (Variable):
            overrange (Variable):
            conversionFactor (Variable):
            config (Variable):
                enabled (Variable):
                gain (Variable):
                offset (Variable):
                alarms (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_LinearPositionMeasurementU32 (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                millimeters (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                micrometers (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newMillimetersValue (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                millimeters (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                micrometers (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newMillimetersValue (Variable):
            rawValue (Variable):
            error (Variable):
            underrange (Variable):
            overrange (Variable):
            conversionFactor (Variable):
            config (Variable):
                enabled (Variable):
                gain (Variable):
                offset (Variable):
                alarms (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_SimpleRelay (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            digitalOutput (Variable):
            isEnabled (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
            processes (Variable):
                setEnabled (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    set (Variable):
                        enabled (Variable):
                    get (Variable):
                        enabled (Variable):
            setEnabled (Method):
                enabled (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_ConfigManager (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            filePath (Variable):
            isEnabled (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            processes (Variable):
                save (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        filePath (Variable):
                    get (Variable):
                        filePath (Variable):
                load (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        filePath (Variable):
                    get (Variable):
                        filePath (Variable):
                activate (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
            save (Method):
                filePath (Variable):
            load (Method):
                filePath (Variable):
            activate (Method):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_ModbusRTUBus (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            retries (Variable):
            isEnabled (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
            processes (Variable):
                readCoil (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value (Variable):
                    errorId (Variable):
                    set (Variable):
                        unitID (Variable):
                        address (Variable):
                    get (Variable):
                        unitID (Variable):
                        address (Variable):
                writeCoil (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        unitID (Variable):
                        address (Variable):
                        value (Variable):
                    get (Variable):
                        unitID (Variable):
                        address (Variable):
                        value (Variable):
                readRegister (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value (Variable):
                    errorId (Variable):
                    set (Variable):
                        unitID (Variable):
                        address (Variable):
                    get (Variable):
                        unitID (Variable):
                        address (Variable):
                writeRegister (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        unitID (Variable):
                        address (Variable):
                        value (Variable):
                    get (Variable):
                        unitID (Variable):
                        address (Variable):
                        value (Variable):
                diagnostics (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value (Variable):
                    errorId (Variable):
                    set (Variable):
                        unitID (Variable):
                        subFunction (Variable):
                        subData (Variable):
                    get (Variable):
                        unitID (Variable):
                        subFunction (Variable):
                        subData (Variable):
            readCoil (Method):
                unitID (Variable):
                address (Variable):
            writeCoil (Method):
                unitID (Variable):
                address (Variable):
                value (Variable):
            readRegister (Method):
                unitID (Variable):
                address (Variable):
            writeRegister (Method):
                unitID (Variable):
                address (Variable):
                value (Variable):
            diagnostics (Method):
                unitID (Variable):
                subFunction (Variable):
                subData (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
    configs (Namespace):
        HiHiLoLoAlarmConfig (Config):
            enabled (Variable):
            loLo (Variable):
            lo (Variable):
            hi (Variable):
            hiHi (Variable):
        MeasurementConfig (Config):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        FaulhaberDriveConfig (Config):
            sendAtInitialization (Variable):
            CANopenBusNetId (Variable):
            CANopenNodePort (Variable):
            modesOfOperation (Variable):
            polarity (Variable):
            maxProfileVelocity (Variable):
            negativeLimitSwitch (Variable):
            positiveLimitSwitch (Variable):
            homingSwitch (Variable):
            limitSwitchPolarity (Variable):
            continuousCurrentLimit (Variable):
            peakCurrentLimit (Variable):
            velocityControlProportionalTerm (Variable):
            velocityControlIntegralTerm (Variable):
        InstrumentConfig (Config):
            name (Variable):
            doInitialThermalFocus (Variable):
            changeM3 (Variable):
            moveKnownPosition (Variable):
            moveKnownPositionName (Variable):
            powerOnRoc (Variable):
            powerOffRoc (Variable):
            powerOnRon (Variable):
            powerOffRon (Variable):
            loadPointingModel (Variable):
            rocActive (Variable):
            ronActive (Variable):
    structs (Namespace):
        HiHiLoLoAlarmConfig (Config):
            enabled (Variable):
            loLo (Variable):
            lo (Variable):
            hi (Variable):
            hiHi (Variable):
        MeasurementConfig (Config):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        BaseProcessStatuses (Struct):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        ProcessStepStatuses (Struct):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
        ChangeSetpointProcessArgs (Struct):
            setpoint (Variable):
        ChangeParameterProcessArgs (Struct):
            parameter (Variable):
        McMoveAbsoluteProcessArgs (Struct):
            position (Variable):
            maxVelocity (Variable):
        McMoveRelativeProcessArgs (Struct):
            distance (Variable):
            maxVelocity (Variable):
        McPowerProcessArgs (Struct):
            enable (Variable):
        McSetPositionProcessArgs (Struct):
            value (Variable):
        McStopProcessArgs (Struct):
            deceleration (Variable):
            jerk (Variable):
        McMoveVelocityProcessArgs (Struct):
            value (Variable):
        SetEnabledProcessArgs (Struct):
            enabled (Variable):
        McWriteParameterArgs (Struct):
            parameterNumber (Variable):
            value (Variable):
        McReadParameterArgs (Struct):
            parameterNumber (Variable):
        McWriteBoolParameterArgs (Struct):
            parameterNumber (Variable):
            value (Variable):
        McReadBoolParameterArgs (Struct):
            parameterNumber (Variable):
        McProbeArgs (Struct):
            risingEdge (Variable):
            timeout (Variable):
        BaseAxisStatuses (Struct):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            poweredStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            extSetGenStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            motionStatus (Variable):
                superState (Variable):
                actVel (Variable):
                tolerance (Variable):
                forward (Variable):
                backward (Variable):
                standstill (Variable):
        SDOReadProcessArgs (Struct):
            index (Variable):
            subindex (Variable):
            noOfBytes (Variable):
        SDOWriteProcessArgs (Struct):
            index (Variable):
            subindex (Variable):
            noOfBytes (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
        FaulhaberDriveConfig (Config):
            sendAtInitialization (Variable):
            CANopenBusNetId (Variable):
            CANopenNodePort (Variable):
            modesOfOperation (Variable):
            polarity (Variable):
            maxProfileVelocity (Variable):
            negativeLimitSwitch (Variable):
            positiveLimitSwitch (Variable):
            homingSwitch (Variable):
            limitSwitchPolarity (Variable):
            continuousCurrentLimit (Variable):
            peakCurrentLimit (Variable):
            velocityControlProportionalTerm (Variable):
            velocityControlIntegralTerm (Variable):
        FaulhaberDriveStatuses (Struct):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        SoEReadProcessArgs (Struct):
            index (Variable):
            noOfBytes (Variable):
        SoEWriteProcessArgs (Struct):
            index (Variable):
            noOfBytes (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
        SetBrakeProcessArgs (Struct):
            newState (Variable):
        AX52XXDriveChannelStatuses (Struct):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        AX52XXDriveStatuses (Struct):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        AX52XXDriveParts (Struct):
            channelA (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                errorC1D (Variable):
                isEnabled (Variable):
                torqueFeedbackValue (Variable):
                torqueCommandValue (Variable):
                digitalInputs (Variable):
                digitalOutputs (Variable):
                safetyOptionState (Variable):
                diagnosticNumber (Variable):
                isInSafetate (Variable):
                actualTorqueFeedback (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newNewtonmeterValue (Variable):
                channelPeakCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                channelRatedCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                continuousStallTorque (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newNewtonmeterValue (Variable):
                continuousStallCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                torqueConstant (Variable):
                bipolarTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                positiveTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                negativeTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                operatingMode (Variable):
                operatingModeDescription (Variable):
                brakeState (Variable):
                brakeStateDescription (Variable):
                torqueCommand (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                safetyErrorAck (Variable):
                statuses (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                processes (Variable):
                    read (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                        set (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                        get (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                    write (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                        set (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                        get (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                    update (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        testVar (Variable):
                    reset (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                    acknowledgeSafetyError (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        testVar (Variable):
                    setBrake (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        set (Variable):
                            newState (Variable):
                        get (Variable):
                            newState (Variable):
            channelB (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                errorC1D (Variable):
                isEnabled (Variable):
                torqueFeedbackValue (Variable):
                torqueCommandValue (Variable):
                digitalInputs (Variable):
                digitalOutputs (Variable):
                safetyOptionState (Variable):
                diagnosticNumber (Variable):
                isInSafetate (Variable):
                actualTorqueFeedback (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newNewtonmeterValue (Variable):
                channelPeakCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                channelRatedCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                continuousStallTorque (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newNewtonmeterValue (Variable):
                continuousStallCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                torqueConstant (Variable):
                bipolarTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                positiveTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                negativeTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                operatingMode (Variable):
                operatingModeDescription (Variable):
                brakeState (Variable):
                brakeStateDescription (Variable):
                torqueCommand (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                safetyErrorAck (Variable):
                statuses (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                processes (Variable):
                    read (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                        set (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                        get (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                    write (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                        set (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                        get (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                    update (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        testVar (Variable):
                    reset (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                    acknowledgeSafetyError (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        testVar (Variable):
                    setBrake (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        set (Variable):
                            newState (Variable):
                        get (Variable):
                            newState (Variable):
        SSIEncoderStatuses (Struct):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        IncrementalEncoderStatuses (Struct):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
        EtherCatDeviceStatuses (Struct):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        CANopenBusStatuses (Struct):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        CurrentMeasurementStatuses (Struct):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        VoltageMeasurementStatuses (Struct):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        TemperatureMeasurementStatuses (Struct):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        PressureMeasurementStatuses (Struct):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        FrequencyMeasurementStatuses (Struct):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        GForceMeasurementStatuses (Struct):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        RelativeHumidityMeasurementStatuses (Struct):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        ForceMeasurementStatuses (Struct):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        LinearPositionMeasurement16Statuses (Struct):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        LinearPositionMeasurementU32Statuses (Struct):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        SimpleRelayStatuses (Struct):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
        ChangeOperatingStateProcessArgs (Struct):
            state (Variable):
        ChangeOperatorStateProcessArgs (Struct):
            state (Variable):
            password (Variable):
        ChangeOperatingModeStateProcessArgs (Struct):
            state (Variable):
        ReadXmlProcessArgs (Struct):
            filePath (Variable):
        WriteXmlProcessArgs (Struct):
            filePath (Variable):
        ConfigManagerStatuses (Struct):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        InstrumentConfig (Config):
            name (Variable):
            doInitialThermalFocus (Variable):
            changeM3 (Variable):
            moveKnownPosition (Variable):
            moveKnownPositionName (Variable):
            powerOnRoc (Variable):
            powerOffRoc (Variable):
            powerOnRon (Variable):
            powerOffRon (Variable):
            loadPointingModel (Variable):
            rocActive (Variable):
            ronActive (Variable):
        ModbusRTUBusReadCoilProcessArgs (Struct):
            unitID (Variable):
            address (Variable):
        ModbusRTUBusWriteCoilProcessArgs (Struct):
            unitID (Variable):
            address (Variable):
            value (Variable):
        ModbusRTUBusReadRegisterProcessArgs (Struct):
            unitID (Variable):
            address (Variable):
        ModbusRTUBusWriteRegisterProcessArgs (Struct):
            unitID (Variable):
            address (Variable):
            value (Variable):
        ModbusRTUBusDiagnosticsProcessArgs (Struct):
            unitID (Variable):
            subFunction (Variable):
            subData (Variable):
        ModbusRTUBusStatuses (Struct):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
    processes (ProcessesNamespace):
        args (Namespace):
            ChangeSetpointProcessArgs (Struct):
                setpoint (Variable):
            ChangeParameterProcessArgs (Struct):
                parameter (Variable):
            McMoveAbsoluteProcessArgs (Struct):
                position (Variable):
                maxVelocity (Variable):
            McMoveRelativeProcessArgs (Struct):
                distance (Variable):
                maxVelocity (Variable):
            McPowerProcessArgs (Struct):
                enable (Variable):
            McSetPositionProcessArgs (Struct):
                value (Variable):
            McStopProcessArgs (Struct):
                deceleration (Variable):
                jerk (Variable):
            McMoveVelocityProcessArgs (Struct):
                value (Variable):
            SetEnabledProcessArgs (Struct):
                enabled (Variable):
            McWriteParameterArgs (Struct):
                parameterNumber (Variable):
                value (Variable):
            McReadParameterArgs (Struct):
                parameterNumber (Variable):
            McWriteBoolParameterArgs (Struct):
                parameterNumber (Variable):
                value (Variable):
            McReadBoolParameterArgs (Struct):
                parameterNumber (Variable):
            McProbeArgs (Struct):
                risingEdge (Variable):
                timeout (Variable):
            SDOReadProcessArgs (Struct):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
            SDOWriteProcessArgs (Struct):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            SoEReadProcessArgs (Struct):
                index (Variable):
                noOfBytes (Variable):
            SoEWriteProcessArgs (Struct):
                index (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            SetBrakeProcessArgs (Struct):
                newState (Variable):
            ChangeOperatingStateProcessArgs (Struct):
                state (Variable):
            ChangeOperatorStateProcessArgs (Struct):
                state (Variable):
                password (Variable):
            ChangeOperatingModeStateProcessArgs (Struct):
                state (Variable):
            ReadXmlProcessArgs (Struct):
                filePath (Variable):
            WriteXmlProcessArgs (Struct):
                filePath (Variable):
            ModbusRTUBusReadCoilProcessArgs (Struct):
                unitID (Variable):
                address (Variable):
            ModbusRTUBusWriteCoilProcessArgs (Struct):
                unitID (Variable):
                address (Variable):
                value (Variable):
            ModbusRTUBusReadRegisterProcessArgs (Struct):
                unitID (Variable):
                address (Variable):
            ModbusRTUBusWriteRegisterProcessArgs (Struct):
                unitID (Variable):
                address (Variable):
                value (Variable):
            ModbusRTUBusDiagnosticsProcessArgs (Struct):
                unitID (Variable):
                subFunction (Variable):
                subData (Variable):
        Process (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            testVar (Variable):
            start (Method):
            request (Method):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            testVar (Variable):
            start (Method):
            request (Method):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ChangeSetpointProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            set (Variable):
                setpoint (Variable):
            get (Variable):
                setpoint (Variable):
            start (Method):
                setpoint (Variable):
            request (Method):
                setpoint (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ChangeParameterProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            set (Variable):
                parameter (Variable):
            get (Variable):
                parameter (Variable):
            start (Method):
                parameter (Variable):
            request (Method):
                parameter (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McMoveAbsoluteProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                position (Variable):
                maxVelocity (Variable):
            get (Variable):
                position (Variable):
                maxVelocity (Variable):
            start (Method):
                position (Variable):
                maxVelocity (Variable):
            request (Method):
                position (Variable):
                maxVelocity (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McMoveRelativeProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                distance (Variable):
                maxVelocity (Variable):
            get (Variable):
                distance (Variable):
                maxVelocity (Variable):
            start (Method):
                distance (Variable):
                maxVelocity (Variable):
            request (Method):
                distance (Variable):
                maxVelocity (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McPowerProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                enable (Variable):
            get (Variable):
                enable (Variable):
            start (Method):
                enable (Variable):
            request (Method):
                enable (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McSetPositionProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                value (Variable):
            get (Variable):
                value (Variable):
            start (Method):
                value (Variable):
            request (Method):
                value (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McStopProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                deceleration (Variable):
                jerk (Variable):
            get (Variable):
                deceleration (Variable):
                jerk (Variable):
            start (Method):
                deceleration (Variable):
                jerk (Variable):
            request (Method):
                deceleration (Variable):
                jerk (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McMoveVelocityProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                value (Variable):
            get (Variable):
                value (Variable):
            start (Method):
                value (Variable):
            request (Method):
                value (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SetEnabledProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            set (Variable):
                enabled (Variable):
            get (Variable):
                enabled (Variable):
            start (Method):
                enabled (Variable):
            request (Method):
                enabled (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McWriteParameter (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                parameterNumber (Variable):
                value (Variable):
            get (Variable):
                parameterNumber (Variable):
                value (Variable):
            start (Method):
                parameterNumber (Variable):
                value (Variable):
            request (Method):
                parameterNumber (Variable):
                value (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McReadParameter (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            value (Variable):
            set (Variable):
                parameterNumber (Variable):
            get (Variable):
                parameterNumber (Variable):
            start (Method):
                parameterNumber (Variable):
            request (Method):
                parameterNumber (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McWriteBoolParameter (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                parameterNumber (Variable):
                value (Variable):
            get (Variable):
                parameterNumber (Variable):
                value (Variable):
            start (Method):
                parameterNumber (Variable):
                value (Variable):
            request (Method):
                parameterNumber (Variable):
                value (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McReadBoolParameter (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            value (Variable):
            set (Variable):
                parameterNumber (Variable):
            get (Variable):
                parameterNumber (Variable):
            start (Method):
                parameterNumber (Variable):
            request (Method):
                parameterNumber (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McProbe (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            recordedPosition (Variable):
            set (Variable):
                risingEdge (Variable):
                timeout (Variable):
            get (Variable):
                risingEdge (Variable):
                timeout (Variable):
            start (Method):
                risingEdge (Variable):
                timeout (Variable):
            request (Method):
                risingEdge (Variable):
                timeout (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SDOReadProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
            errorId (Variable):
            set (Variable):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
            get (Variable):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
            start (Method):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
            request (Method):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SDOWriteProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            get (Variable):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            start (Method):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            request (Method):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SoEReadProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
            adsErrorId (Variable):
            sercosErrorId (Variable):
            set (Variable):
                index (Variable):
                noOfBytes (Variable):
            get (Variable):
                index (Variable):
                noOfBytes (Variable):
            start (Method):
                index (Variable):
                noOfBytes (Variable):
            request (Method):
                index (Variable):
                noOfBytes (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SoEWriteProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            adsErrorId (Variable):
            sercosErrorId (Variable):
            set (Variable):
                index (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            get (Variable):
                index (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            start (Method):
                index (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            request (Method):
                index (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SoEResetProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            adsErrorId (Variable):
            sercosErrorId (Variable):
            start (Method):
            request (Method):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SetBrakeProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            set (Variable):
                newState (Variable):
            get (Variable):
                newState (Variable):
            start (Method):
                newState (Variable):
            request (Method):
                newState (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ChangeOperatingStateProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            set (Variable):
                state (Variable):
            get (Variable):
                state (Variable):
            start (Method):
                state (Variable):
            request (Method):
                state (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ChangeOperatorStateProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            set (Variable):
                state (Variable):
                password (Variable):
            get (Variable):
                state (Variable):
                password (Variable):
            start (Method):
                state (Variable):
                password (Variable):
            request (Method):
                state (Variable):
                password (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ChangeOperatingModeStateProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            set (Variable):
                state (Variable):
            get (Variable):
                state (Variable):
            start (Method):
                state (Variable):
            request (Method):
                state (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ReadXmlProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseXmlDataSrvProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                filePath (Variable):
            get (Variable):
                filePath (Variable):
            start (Method):
                filePath (Variable):
            request (Method):
                filePath (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        WriteXmlProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseXmlDataSrvProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                filePath (Variable):
            get (Variable):
                filePath (Variable):
            start (Method):
                filePath (Variable):
            request (Method):
                filePath (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ActivateProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            testVar (Variable):
            start (Method):
            request (Method):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ModbusRTUBusReadCoilProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            value (Variable):
            errorId (Variable):
            set (Variable):
                unitID (Variable):
                address (Variable):
            get (Variable):
                unitID (Variable):
                address (Variable):
            start (Method):
                unitID (Variable):
                address (Variable):
            request (Method):
                unitID (Variable):
                address (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ModbusRTUBusWriteCoilProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                unitID (Variable):
                address (Variable):
                value (Variable):
            get (Variable):
                unitID (Variable):
                address (Variable):
                value (Variable):
            start (Method):
                unitID (Variable):
                address (Variable):
                value (Variable):
            request (Method):
                unitID (Variable):
                address (Variable):
                value (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ModbusRTUBusReadRegisterProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            value (Variable):
            errorId (Variable):
            set (Variable):
                unitID (Variable):
                address (Variable):
            get (Variable):
                unitID (Variable):
                address (Variable):
            start (Method):
                unitID (Variable):
                address (Variable):
            request (Method):
                unitID (Variable):
                address (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ModbusRTUBusWriteRegisterProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                unitID (Variable):
                address (Variable):
                value (Variable):
            get (Variable):
                unitID (Variable):
                address (Variable):
                value (Variable):
            start (Method):
                unitID (Variable):
                address (Variable):
                value (Variable):
            request (Method):
                unitID (Variable):
                address (Variable):
                value (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ModbusRTUBusDiagnosticsProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            value (Variable):
            errorId (Variable):
            set (Variable):
                unitID (Variable):
                subFunction (Variable):
                subData (Variable):
            get (Variable):
                unitID (Variable):
                subFunction (Variable):
                subData (Variable):
            start (Method):
                unitID (Variable):
                subFunction (Variable):
                subData (Variable):
            request (Method):
                unitID (Variable):
                subFunction (Variable):
                subData (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
    functionblocks (Namespace):
        ApertureStatus (Status):
            superState (Variable):
            isOpen (Variable):
            isClosed (Variable):
            open (Variable):
            closed (Variable):
            partiallyOpen (Variable):
            undefined (Variable):
        BusyStatus (Status):
            superState (Variable):
            isBusy (Variable):
            idle (Variable):
            busy (Variable):
        StartedStatus (Status):
            superState (Variable):
            isStarted (Variable):
            started (Variable):
            notStarted (Variable):
        EnabledStatus (Status):
            superState (Variable):
            isEnabled (Variable):
            enabled (Variable):
            disabled (Variable):
        PoweredStatus (Status):
            superState (Variable):
            isEnabled (Variable):
            enabled (Variable):
            disabled (Variable):
        HealthStatus (Status):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
        InitializationStatus (Status):
            superState (Variable):
            state (Variable):
            shutdown (Variable):
            initializing (Variable):
            initializingFailed (Variable):
            initialized (Variable):
            locked (Variable):
        MotionStatus (Status):
            superState (Variable):
            actVel (Variable):
            tolerance (Variable):
            forward (Variable):
            backward (Variable):
            standstill (Variable):
        HiHiLoLoAlarmStatus (Status):
            superState (Variable):
            config (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
            value (Variable):
            disabled (Variable):
            hiHi (Variable):
            hi (Variable):
            ok (Variable):
            lo (Variable):
            loLo (Variable):
        OpeningStatus (Status):
            superState (Variable):
            isOpening (Variable):
            isClosing (Variable):
            opening (Variable):
            closing (Variable):
            standstill (Variable):
        OperatorStatus (Status):
            superState (Variable):
            state (Variable):
            none (Variable):
            tech (Variable):
            observer (Variable):
        OperatingStatus (Status):
            superState (Variable):
            state (Variable):
            auto (Variable):
            manual (Variable):
        OperatingModeStatus (Status):
            superState (Variable):
            state (Variable):
            local (Variable):
            remote (Variable):
        ActivityStatus (Status):
            superState (Variable):
            isMoving (Variable):
            isAwake (Variable):
            moving (Variable):
            awake (Variable):
            sleeping (Variable):
        SM_QuantityValue (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_AngularPosition (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            radians (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            degrees (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            arcseconds (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newDegreesValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_LinearPosition (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            millimeters (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            micrometers (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMillimetersValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_AngularVelocity (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            radiansPerSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            degreesPerSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            arcsecondsPerSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newDegreesPerSecondValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_AngularAcceleration (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            radiansPerSquareSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            degreesPerSquareSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            arcsecondsPerSquareSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newDegreesPerSquareSecondValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_LinearVelocity (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            millimetersPerSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMillimetersPerSecondValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Voltage (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            volt (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliVolt (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newVoltValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_GForce (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            g (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliG (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMilliGValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Current (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            amps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliAmps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newAmpsValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Torque (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            newtonmeter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newNewtonmeterValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_TorqueLimit (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            unitless (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            percent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newtonmeter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newFractionValue (Variable):
            maxNewtonmeter (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_AverageCurrent (Statemachine):
            SUPER (Pointer):
               points_to_type: Current
            actualStatus (Variable):
            previousStatus (Variable):
            amps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliAmps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newAmpsValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Duration (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            seconds (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            minutes (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newSecondsValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Temperature (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            celsius (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            kelvin (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newCelsiusValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Pressure (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            bar (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            pascal (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            hectoPascal (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newBarValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Frequency (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            hertz (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newHertzValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Force (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            newton (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            decaNewton (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newNewtonValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_Fraction (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            unitless (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            percent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newFractionValue (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_BaseProcess (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_BaseMcProcess (Statemachine):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
        SM_ProcessStep (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            process (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_BaseXmlDataSrvProcess (Statemachine):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
        Process (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            testVar (Variable):
            start (Method):
            request (Method):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            testVar (Variable):
            start (Method):
            request (Method):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ChangeSetpointProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            set (Variable):
                setpoint (Variable):
            get (Variable):
                setpoint (Variable):
            start (Method):
                setpoint (Variable):
            request (Method):
                setpoint (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ChangeParameterProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            set (Variable):
                parameter (Variable):
            get (Variable):
                parameter (Variable):
            start (Method):
                parameter (Variable):
            request (Method):
                parameter (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McMoveAbsoluteProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                position (Variable):
                maxVelocity (Variable):
            get (Variable):
                position (Variable):
                maxVelocity (Variable):
            start (Method):
                position (Variable):
                maxVelocity (Variable):
            request (Method):
                position (Variable):
                maxVelocity (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McMoveRelativeProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                distance (Variable):
                maxVelocity (Variable):
            get (Variable):
                distance (Variable):
                maxVelocity (Variable):
            start (Method):
                distance (Variable):
                maxVelocity (Variable):
            request (Method):
                distance (Variable):
                maxVelocity (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McPowerProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                enable (Variable):
            get (Variable):
                enable (Variable):
            start (Method):
                enable (Variable):
            request (Method):
                enable (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McSetPositionProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                value (Variable):
            get (Variable):
                value (Variable):
            start (Method):
                value (Variable):
            request (Method):
                value (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McStopProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                deceleration (Variable):
                jerk (Variable):
            get (Variable):
                deceleration (Variable):
                jerk (Variable):
            start (Method):
                deceleration (Variable):
                jerk (Variable):
            request (Method):
                deceleration (Variable):
                jerk (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McMoveVelocityProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                value (Variable):
            get (Variable):
                value (Variable):
            start (Method):
                value (Variable):
            request (Method):
                value (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SetEnabledProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            set (Variable):
                enabled (Variable):
            get (Variable):
                enabled (Variable):
            start (Method):
                enabled (Variable):
            request (Method):
                enabled (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McWriteParameter (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                parameterNumber (Variable):
                value (Variable):
            get (Variable):
                parameterNumber (Variable):
                value (Variable):
            start (Method):
                parameterNumber (Variable):
                value (Variable):
            request (Method):
                parameterNumber (Variable):
                value (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McReadParameter (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            value (Variable):
            set (Variable):
                parameterNumber (Variable):
            get (Variable):
                parameterNumber (Variable):
            start (Method):
                parameterNumber (Variable):
            request (Method):
                parameterNumber (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McWriteBoolParameter (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                parameterNumber (Variable):
                value (Variable):
            get (Variable):
                parameterNumber (Variable):
                value (Variable):
            start (Method):
                parameterNumber (Variable):
                value (Variable):
            request (Method):
                parameterNumber (Variable):
                value (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McReadBoolParameter (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            value (Variable):
            set (Variable):
                parameterNumber (Variable):
            get (Variable):
                parameterNumber (Variable):
            start (Method):
                parameterNumber (Variable):
            request (Method):
                parameterNumber (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        McProbe (Process):
            SUPER (Pointer):
               points_to_type: BaseMcProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            recordedPosition (Variable):
            set (Variable):
                risingEdge (Variable):
                timeout (Variable):
            get (Variable):
                risingEdge (Variable):
                timeout (Variable):
            start (Method):
                risingEdge (Variable):
                timeout (Variable):
            request (Method):
                risingEdge (Variable):
                timeout (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SM_BaseAxis (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            axis_ref (Variable):
                PlcToNc (Variable):
                    ControlDWord (Variable):
                    Override (Variable):
                    AxisModeRequest (Variable):
                    AxisModeDWord (Variable):
                    AxisModeLReal (Variable):
                    PositionCorrection (Variable):
                    ExtSetPos (Variable):
                    ExtSetVelo (Variable):
                    ExtSetAcc (Variable):
                    ExtSetDirection (Variable):
                    ExtControllerOutput (Variable):
                    GearRatio1 (Variable):
                    GearRatio2 (Variable):
                    GearRatio3 (Variable):
                    GearRatio4 (Variable):
                NcToPlc (Variable):
                    StateDWord (Variable):
                    ErrorCode (Variable):
                    AxisState (Variable):
                    AxisModeConfirmation (Variable):
                    HomingState (Variable):
                    CoupleState (Variable):
                    SvbEntries (Variable):
                    SafEntries (Variable):
                    AxisId (Variable):
                    OpModeDWord (Variable):
                    ActiveControlLoopIndex (Variable):
                    ControlLoopIndex (Variable):
                    ActPos (Variable):
                    ModuloActPos (Variable):
                    ModuloActTurns (Variable):
                    ActVelo (Variable):
                    PosDiff (Variable):
                    SetPos (Variable):
                    SetVelo (Variable):
                    SetAcc (Variable):
                    TargetPos (Variable):
                    ModuloSetPos (Variable):
                    ModuloSetTurns (Variable):
                    CmdNo (Variable):
                    CmdState (Variable):
                Status (Variable):
                    UpdateTaskIndex (Variable):
                    UpdateCycleTime (Variable):
                    CycleCounter (Variable):
                    NcCycleCounter (Variable):
                    Error (Variable):
                    ErrorId (Variable):
                    Disabled (Variable):
                    ControlLoopClosed (Variable):
                    ExtSetPointGenEnabled (Variable):
            isGearingSupported (Variable):
            isJogEnabled (Variable):
            isEnabled (Variable):
            standstillTolerance (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                poweredStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                extSetGenStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                motionStatus (Variable):
                    superState (Variable):
                    actVel (Variable):
                    tolerance (Variable):
                    forward (Variable):
                    backward (Variable):
                    standstill (Variable):
            processes (Variable):
                moveAbsolute (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        position (Variable):
                        maxVelocity (Variable):
                    get (Variable):
                        position (Variable):
                        maxVelocity (Variable):
                moveRelative (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        distance (Variable):
                        maxVelocity (Variable):
                    get (Variable):
                        distance (Variable):
                        maxVelocity (Variable):
                moveVelocity (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        value (Variable):
                    get (Variable):
                        value (Variable):
                reset (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                stop (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                stopParametrized (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        deceleration (Variable):
                        jerk (Variable):
                    get (Variable):
                        deceleration (Variable):
                        jerk (Variable):
                power (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        enable (Variable):
                    get (Variable):
                        enable (Variable):
                gearIn (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                gearOut (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                initialize (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                setPosition (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        value (Variable):
                    get (Variable):
                        value (Variable):
                enableExtSetGen (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                disableExtSetGen (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                forceCalibration (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                resetCalibration (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                readParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    value (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                readBoolParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    value (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                writeParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                writeBoolParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                probeStart (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    recordedPosition (Variable):
                    set (Variable):
                        risingEdge (Variable):
                        timeout (Variable):
                    get (Variable):
                        risingEdge (Variable):
                        timeout (Variable):
                probeAbort (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
            moveAbsolute (Method):
                position (Variable):
                maxVelocity (Variable):
            moveRelative (Method):
                distance (Variable):
                maxVelocity (Variable):
            moveVelocity (Method):
                value (Variable):
            reset (Method):
            stop (Method):
            stopParametrized (Method):
                deceleration (Variable):
                jerk (Variable):
            power (Method):
                enable (Variable):
            gearIn (Method):
            gearOut (Method):
            initialize (Method):
            setPosition (Method):
                value (Variable):
            enableExtSetGen (Method):
            disableExtSetGen (Method):
            forceCalibration (Method):
            resetCalibration (Method):
            readParameter (Method):
                parameterNumber (Variable):
            readBoolParameter (Method):
                parameterNumber (Variable):
            writeParameter (Method):
                parameterNumber (Variable):
                value (Variable):
            writeBoolParameter (Method):
                parameterNumber (Variable):
                value (Variable):
            probeStart (Method):
                risingEdge (Variable):
                timeout (Variable):
            probeAbort (Method):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_AngularAxis (Statemachine):
            SUPER (Pointer):
               points_to_type: BaseAxis
            actualStatus (Variable):
            previousStatus (Variable):
            axis_ref (Variable):
                PlcToNc (Variable):
                    ControlDWord (Variable):
                    Override (Variable):
                    AxisModeRequest (Variable):
                    AxisModeDWord (Variable):
                    AxisModeLReal (Variable):
                    PositionCorrection (Variable):
                    ExtSetPos (Variable):
                    ExtSetVelo (Variable):
                    ExtSetAcc (Variable):
                    ExtSetDirection (Variable):
                    ExtControllerOutput (Variable):
                    GearRatio1 (Variable):
                    GearRatio2 (Variable):
                    GearRatio3 (Variable):
                    GearRatio4 (Variable):
                NcToPlc (Variable):
                    StateDWord (Variable):
                    ErrorCode (Variable):
                    AxisState (Variable):
                    AxisModeConfirmation (Variable):
                    HomingState (Variable):
                    CoupleState (Variable):
                    SvbEntries (Variable):
                    SafEntries (Variable):
                    AxisId (Variable):
                    OpModeDWord (Variable):
                    ActiveControlLoopIndex (Variable):
                    ControlLoopIndex (Variable):
                    ActPos (Variable):
                    ModuloActPos (Variable):
                    ModuloActTurns (Variable):
                    ActVelo (Variable):
                    PosDiff (Variable):
                    SetPos (Variable):
                    SetVelo (Variable):
                    SetAcc (Variable):
                    TargetPos (Variable):
                    ModuloSetPos (Variable):
                    ModuloSetTurns (Variable):
                    CmdNo (Variable):
                    CmdState (Variable):
                Status (Variable):
                    UpdateTaskIndex (Variable):
                    UpdateCycleTime (Variable):
                    CycleCounter (Variable):
                    NcCycleCounter (Variable):
                    Error (Variable):
                    ErrorId (Variable):
                    Disabled (Variable):
                    ControlLoopClosed (Variable):
                    ExtSetPointGenEnabled (Variable):
            isGearingSupported (Variable):
            isJogEnabled (Variable):
            isEnabled (Variable):
            standstillTolerance (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                poweredStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                extSetGenStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                motionStatus (Variable):
                    superState (Variable):
                    actVel (Variable):
                    tolerance (Variable):
                    forward (Variable):
                    backward (Variable):
                    standstill (Variable):
            processes (Variable):
                moveAbsolute (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        position (Variable):
                        maxVelocity (Variable):
                    get (Variable):
                        position (Variable):
                        maxVelocity (Variable):
                moveRelative (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        distance (Variable):
                        maxVelocity (Variable):
                    get (Variable):
                        distance (Variable):
                        maxVelocity (Variable):
                moveVelocity (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        value (Variable):
                    get (Variable):
                        value (Variable):
                reset (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                stop (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                stopParametrized (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        deceleration (Variable):
                        jerk (Variable):
                    get (Variable):
                        deceleration (Variable):
                        jerk (Variable):
                power (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        enable (Variable):
                    get (Variable):
                        enable (Variable):
                gearIn (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                gearOut (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                initialize (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                setPosition (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        value (Variable):
                    get (Variable):
                        value (Variable):
                enableExtSetGen (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                disableExtSetGen (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                forceCalibration (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                resetCalibration (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                readParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    value (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                readBoolParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    value (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                writeParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                writeBoolParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                probeStart (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    recordedPosition (Variable):
                    set (Variable):
                        risingEdge (Variable):
                        timeout (Variable):
                    get (Variable):
                        risingEdge (Variable):
                        timeout (Variable):
                probeAbort (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
            moveAbsolute (Method):
                position (Variable):
                maxVelocity (Variable):
            moveRelative (Method):
                distance (Variable):
                maxVelocity (Variable):
            moveVelocity (Method):
                value (Variable):
            reset (Method):
            stop (Method):
            stopParametrized (Method):
                deceleration (Variable):
                jerk (Variable):
            power (Method):
                enable (Variable):
            gearIn (Method):
            gearOut (Method):
            initialize (Method):
            setPosition (Method):
                value (Variable):
            enableExtSetGen (Method):
            disableExtSetGen (Method):
            forceCalibration (Method):
            resetCalibration (Method):
            readParameter (Method):
                parameterNumber (Variable):
            readBoolParameter (Method):
                parameterNumber (Variable):
            writeParameter (Method):
                parameterNumber (Variable):
                value (Variable):
            writeBoolParameter (Method):
                parameterNumber (Variable):
                value (Variable):
            probeStart (Method):
                risingEdge (Variable):
                timeout (Variable):
            probeAbort (Method):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            actPos (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                radians (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                degrees (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                arcseconds (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newDegreesValue (Variable):
            actVel (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                radiansPerSecond (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                degreesPerSecond (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                arcsecondsPerSecond (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newDegreesPerSecondValue (Variable):
        SM_LinearAxis (Statemachine):
            SUPER (Pointer):
               points_to_type: BaseAxis
            actualStatus (Variable):
            previousStatus (Variable):
            axis_ref (Variable):
                PlcToNc (Variable):
                    ControlDWord (Variable):
                    Override (Variable):
                    AxisModeRequest (Variable):
                    AxisModeDWord (Variable):
                    AxisModeLReal (Variable):
                    PositionCorrection (Variable):
                    ExtSetPos (Variable):
                    ExtSetVelo (Variable):
                    ExtSetAcc (Variable):
                    ExtSetDirection (Variable):
                    ExtControllerOutput (Variable):
                    GearRatio1 (Variable):
                    GearRatio2 (Variable):
                    GearRatio3 (Variable):
                    GearRatio4 (Variable):
                NcToPlc (Variable):
                    StateDWord (Variable):
                    ErrorCode (Variable):
                    AxisState (Variable):
                    AxisModeConfirmation (Variable):
                    HomingState (Variable):
                    CoupleState (Variable):
                    SvbEntries (Variable):
                    SafEntries (Variable):
                    AxisId (Variable):
                    OpModeDWord (Variable):
                    ActiveControlLoopIndex (Variable):
                    ControlLoopIndex (Variable):
                    ActPos (Variable):
                    ModuloActPos (Variable):
                    ModuloActTurns (Variable):
                    ActVelo (Variable):
                    PosDiff (Variable):
                    SetPos (Variable):
                    SetVelo (Variable):
                    SetAcc (Variable):
                    TargetPos (Variable):
                    ModuloSetPos (Variable):
                    ModuloSetTurns (Variable):
                    CmdNo (Variable):
                    CmdState (Variable):
                Status (Variable):
                    UpdateTaskIndex (Variable):
                    UpdateCycleTime (Variable):
                    CycleCounter (Variable):
                    NcCycleCounter (Variable):
                    Error (Variable):
                    ErrorId (Variable):
                    Disabled (Variable):
                    ControlLoopClosed (Variable):
                    ExtSetPointGenEnabled (Variable):
            isGearingSupported (Variable):
            isJogEnabled (Variable):
            isEnabled (Variable):
            standstillTolerance (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                poweredStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                extSetGenStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                motionStatus (Variable):
                    superState (Variable):
                    actVel (Variable):
                    tolerance (Variable):
                    forward (Variable):
                    backward (Variable):
                    standstill (Variable):
            processes (Variable):
                moveAbsolute (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        position (Variable):
                        maxVelocity (Variable):
                    get (Variable):
                        position (Variable):
                        maxVelocity (Variable):
                moveRelative (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        distance (Variable):
                        maxVelocity (Variable):
                    get (Variable):
                        distance (Variable):
                        maxVelocity (Variable):
                moveVelocity (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        value (Variable):
                    get (Variable):
                        value (Variable):
                reset (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                stop (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                stopParametrized (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        deceleration (Variable):
                        jerk (Variable):
                    get (Variable):
                        deceleration (Variable):
                        jerk (Variable):
                power (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        enable (Variable):
                    get (Variable):
                        enable (Variable):
                gearIn (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                gearOut (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                initialize (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                setPosition (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        value (Variable):
                    get (Variable):
                        value (Variable):
                enableExtSetGen (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                disableExtSetGen (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                forceCalibration (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                resetCalibration (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
                readParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    value (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                readBoolParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    value (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                writeParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                writeBoolParameter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                    get (Variable):
                        parameterNumber (Variable):
                        value (Variable):
                probeStart (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    recordedPosition (Variable):
                    set (Variable):
                        risingEdge (Variable):
                        timeout (Variable):
                    get (Variable):
                        risingEdge (Variable):
                        timeout (Variable):
                probeAbort (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    testVar (Variable):
            moveAbsolute (Method):
                position (Variable):
                maxVelocity (Variable):
            moveRelative (Method):
                distance (Variable):
                maxVelocity (Variable):
            moveVelocity (Method):
                value (Variable):
            reset (Method):
            stop (Method):
            stopParametrized (Method):
                deceleration (Variable):
                jerk (Variable):
            power (Method):
                enable (Variable):
            gearIn (Method):
            gearOut (Method):
            initialize (Method):
            setPosition (Method):
                value (Variable):
            enableExtSetGen (Method):
            disableExtSetGen (Method):
            forceCalibration (Method):
            resetCalibration (Method):
            readParameter (Method):
                parameterNumber (Variable):
            readBoolParameter (Method):
                parameterNumber (Variable):
            writeParameter (Method):
                parameterNumber (Variable):
                value (Variable):
            writeBoolParameter (Method):
                parameterNumber (Variable):
                value (Variable):
            probeStart (Method):
                risingEdge (Variable):
                timeout (Variable):
            probeAbort (Method):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            actPos (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                millimeters (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                micrometers (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newMillimetersValue (Variable):
            actVel (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                millimetersPerSecond (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newMillimetersPerSecondValue (Variable):
        SDOReadProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
            errorId (Variable):
            set (Variable):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
            get (Variable):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
            start (Method):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
            request (Method):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SDOWriteProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            get (Variable):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            start (Method):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            request (Method):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SM_FaulhaberDrive (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            config (Variable):
                sendAtInitialization (Variable):
                CANopenBusNetId (Variable):
                CANopenNodePort (Variable):
                modesOfOperation (Variable):
                polarity (Variable):
                maxProfileVelocity (Variable):
                negativeLimitSwitch (Variable):
                positiveLimitSwitch (Variable):
                homingSwitch (Variable):
                limitSwitchPolarity (Variable):
                continuousCurrentLimit (Variable):
                peakCurrentLimit (Variable):
                velocityControlProportionalTerm (Variable):
                velocityControlIntegralTerm (Variable):
            actualCurrent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            averageCurrent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            isEnabled (Variable):
            newMilliampsValue (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            processes (Variable):
                write (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        index (Variable):
                        subindex (Variable):
                        noOfBytes (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                    get (Variable):
                        index (Variable):
                        subindex (Variable):
                        noOfBytes (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                read (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value1Byte (Variable):
                    value2Bytes (Variable):
                    value4Bytes (Variable):
                    errorId (Variable):
                    set (Variable):
                        index (Variable):
                        subindex (Variable):
                        noOfBytes (Variable):
                    get (Variable):
                        index (Variable):
                        subindex (Variable):
                        noOfBytes (Variable):
                initialize (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
            write (Method):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            read (Method):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
            initialize (Method):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SoEReadProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
            adsErrorId (Variable):
            sercosErrorId (Variable):
            set (Variable):
                index (Variable):
                noOfBytes (Variable):
            get (Variable):
                index (Variable):
                noOfBytes (Variable):
            start (Method):
                index (Variable):
                noOfBytes (Variable):
            request (Method):
                index (Variable):
                noOfBytes (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SoEWriteProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            adsErrorId (Variable):
            sercosErrorId (Variable):
            set (Variable):
                index (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            get (Variable):
                index (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            start (Method):
                index (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            request (Method):
                index (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SoEResetProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            adsErrorId (Variable):
            sercosErrorId (Variable):
            start (Method):
            request (Method):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SetBrakeProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            set (Variable):
                newState (Variable):
            get (Variable):
                newState (Variable):
            start (Method):
                newState (Variable):
            request (Method):
                newState (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SM_AX52XXDriveChannel (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            errorC1D (Variable):
            isEnabled (Variable):
            torqueFeedbackValue (Variable):
            torqueCommandValue (Variable):
            digitalInputs (Variable):
            digitalOutputs (Variable):
            safetyOptionState (Variable):
            diagnosticNumber (Variable):
            isInSafetate (Variable):
            actualTorqueFeedback (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newNewtonmeterValue (Variable):
            channelPeakCurrent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            channelRatedCurrent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            continuousStallTorque (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newNewtonmeterValue (Variable):
            continuousStallCurrent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            torqueConstant (Variable):
            bipolarTorqueLimit (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
                maxNewtonmeter (Variable):
            positiveTorqueLimit (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
                maxNewtonmeter (Variable):
            negativeTorqueLimit (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
                maxNewtonmeter (Variable):
            operatingMode (Variable):
            operatingModeDescription (Variable):
            brakeState (Variable):
            brakeStateDescription (Variable):
            torqueCommand (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
                maxNewtonmeter (Variable):
            safetyErrorAck (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            processes (Variable):
                read (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value1Byte (Variable):
                    value2Bytes (Variable):
                    value4Bytes (Variable):
                    adsErrorId (Variable):
                    sercosErrorId (Variable):
                    set (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                    get (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                write (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    adsErrorId (Variable):
                    sercosErrorId (Variable):
                    set (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                    get (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                update (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                reset (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    adsErrorId (Variable):
                    sercosErrorId (Variable):
                acknowledgeSafetyError (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                setBrake (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    set (Variable):
                        newState (Variable):
                    get (Variable):
                        newState (Variable):
            read (Method):
                index (Variable):
                noOfBytes (Variable):
            write (Method):
                index (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            update (Method):
            reset (Method):
            acknowledgeSafetyError (Method):
            setBrake (Method):
                newState (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_AX52XXDrive (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            parts (Variable):
                channelA (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    errorC1D (Variable):
                    isEnabled (Variable):
                    torqueFeedbackValue (Variable):
                    torqueCommandValue (Variable):
                    digitalInputs (Variable):
                    digitalOutputs (Variable):
                    safetyOptionState (Variable):
                    diagnosticNumber (Variable):
                    isInSafetate (Variable):
                    actualTorqueFeedback (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newNewtonmeterValue (Variable):
                    channelPeakCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    channelRatedCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    continuousStallTorque (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newNewtonmeterValue (Variable):
                    continuousStallCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    torqueConstant (Variable):
                    bipolarTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    positiveTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    negativeTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    operatingMode (Variable):
                    operatingModeDescription (Variable):
                    brakeState (Variable):
                    brakeStateDescription (Variable):
                    torqueCommand (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    safetyErrorAck (Variable):
                    statuses (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                    processes (Variable):
                        read (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                            set (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                            get (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                        write (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                            set (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                                value1Byte (Variable):
                                value2Bytes (Variable):
                                value4Bytes (Variable):
                            get (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                                value1Byte (Variable):
                                value2Bytes (Variable):
                                value4Bytes (Variable):
                        update (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            testVar (Variable):
                        reset (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                        acknowledgeSafetyError (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            testVar (Variable):
                        setBrake (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            set (Variable):
                                newState (Variable):
                            get (Variable):
                                newState (Variable):
                channelB (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    errorC1D (Variable):
                    isEnabled (Variable):
                    torqueFeedbackValue (Variable):
                    torqueCommandValue (Variable):
                    digitalInputs (Variable):
                    digitalOutputs (Variable):
                    safetyOptionState (Variable):
                    diagnosticNumber (Variable):
                    isInSafetate (Variable):
                    actualTorqueFeedback (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newNewtonmeterValue (Variable):
                    channelPeakCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    channelRatedCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    continuousStallTorque (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newNewtonmeterValue (Variable):
                    continuousStallCurrent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        amps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        milliAmps (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newAmpsValue (Variable):
                    torqueConstant (Variable):
                    bipolarTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    positiveTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    negativeTorqueLimit (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    operatingMode (Variable):
                    operatingModeDescription (Variable):
                    brakeState (Variable):
                    brakeStateDescription (Variable):
                    torqueCommand (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        unitless (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        percent (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newtonmeter (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            value (Variable):
                            unit (Variable):
                        newFractionValue (Variable):
                        maxNewtonmeter (Variable):
                    safetyErrorAck (Variable):
                    statuses (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                    processes (Variable):
                        read (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                            set (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                            get (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                        write (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                            set (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                                value1Byte (Variable):
                                value2Bytes (Variable):
                                value4Bytes (Variable):
                            get (Variable):
                                index (Variable):
                                noOfBytes (Variable):
                                value1Byte (Variable):
                                value2Bytes (Variable):
                                value4Bytes (Variable):
                        update (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            testVar (Variable):
                        reset (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            adsErrorId (Variable):
                            sercosErrorId (Variable):
                        acknowledgeSafetyError (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            testVar (Variable):
                        setBrake (Variable):
                            actualStatus (Variable):
                            previousStatus (Variable):
                            isEnabled (Variable):
                            statuses (Variable):
                                enabledStatus (Variable):
                                    superState (Variable):
                                    isEnabled (Variable):
                                    enabled (Variable):
                                    disabled (Variable):
                                busyStatus (Variable):
                                    superState (Variable):
                                    isBusy (Variable):
                                    idle (Variable):
                                    busy (Variable):
                                healthStatus (Variable):
                                    superState (Variable):
                                    isGood (Variable):
                                    hasWarning (Variable):
                                    good (Variable):
                                    warning (Variable):
                                    bad (Variable):
                                startedStatus (Variable):
                                    superState (Variable):
                                    isStarted (Variable):
                                    started (Variable):
                                    notStarted (Variable):
                            do_request (Variable):
                            do_request_result (Variable):
                            set (Variable):
                                newState (Variable):
                            get (Variable):
                                newState (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_SSIEncoder (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            dataError (Variable):
            frameError (Variable):
            powerFailure (Variable):
            syncError (Variable):
            counterValue (Variable):
            status (Variable):
            statuses (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_IncrementalEncoder (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            latchCValid (Variable):
            latchExternValid (Variable):
            setCounterDone (Variable):
            counterUnderflow (Variable):
            counterOverflow (Variable):
            statusOfInputStatus (Variable):
            openCircuit (Variable):
            extrapolationStall (Variable):
            statusOfInputA (Variable):
            statusOfInputB (Variable):
            statusOfInputC (Variable):
            statusOfInputGate (Variable):
            statusOfExternLatch (Variable):
            syncError (Variable):
            isEnabled (Variable):
            counterValue (Variable):
            status (Variable):
            setCounterValue (Variable):
            setCounter (Variable):
            enableLatchC (Variable):
            statuses (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
            processes (Variable):
                enableCounterResetC (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                disableCounterResetC (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
            enableCounterResetC (Method):
            disableCounterResetC (Method):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_EtherCatDevice (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            id (Variable):
            typeId (Variable):
            wcState (Variable):
            infoData (Variable):
            statuses (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_CANopenBus (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            error (Variable):
            canState (Variable):
            rxErrorCounter (Variable):
            txErrorCounter (Variable):
            statuses (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_CurrentMeasurement (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            current (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            microAmpsValue (Variable):
            error (Variable):
            statuses (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_VoltageMeasurement (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                volt (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliVolt (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newVoltValue (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                volt (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliVolt (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newVoltValue (Variable):
            underrange (Variable):
            overrange (Variable):
            rawValue (Variable):
            error (Variable):
            conversionFactor (Variable):
            config (Variable):
                enabled (Variable):
                gain (Variable):
                offset (Variable):
                alarms (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_TemperatureMeasurement (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                celsius (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                kelvin (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newCelsiusValue (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                celsius (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                kelvin (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newCelsiusValue (Variable):
            underrange (Variable):
            overrange (Variable):
            rawValue (Variable):
            error (Variable):
            conversionFactor (Variable):
            config (Variable):
                enabled (Variable):
                gain (Variable):
                offset (Variable):
                alarms (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_PressureMeasurement (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                bar (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                pascal (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                hectoPascal (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newBarValue (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                bar (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                pascal (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                hectoPascal (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newBarValue (Variable):
            underrange (Variable):
            overrange (Variable):
            rawValue (Variable):
            error (Variable):
            conversionFactor (Variable):
            config (Variable):
                enabled (Variable):
                gain (Variable):
                offset (Variable):
                alarms (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_FrequencyMeasurement (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                hertz (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newHertzValue (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                hertz (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newHertzValue (Variable):
            underrange (Variable):
            overrange (Variable):
            rawValue (Variable):
            error (Variable):
            conversionFactor (Variable):
            config (Variable):
                enabled (Variable):
                gain (Variable):
                offset (Variable):
                alarms (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_GForceMeasurement (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                g (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliG (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newMilliGValue (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                g (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliG (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newMilliGValue (Variable):
            milliGValue (Variable):
            error (Variable):
            statuses (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_RelativeHumidityMeasurement (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
            underrange (Variable):
            overrange (Variable):
            rawValue (Variable):
            error (Variable):
            conversionFactor (Variable):
            config (Variable):
                enabled (Variable):
                gain (Variable):
                offset (Variable):
                alarms (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_ForceMeasurement (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                newton (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                decaNewton (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newNewtonValue (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                newton (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                decaNewton (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newNewtonValue (Variable):
            rawValue (Variable):
            error (Variable):
            underrange (Variable):
            overrange (Variable):
            conversionFactor (Variable):
            config (Variable):
                enabled (Variable):
                gain (Variable):
                offset (Variable):
                alarms (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_LinearPositionMeasurement16 (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                millimeters (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                micrometers (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newMillimetersValue (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                millimeters (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                micrometers (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newMillimetersValue (Variable):
            rawValue (Variable):
            error (Variable):
            underrange (Variable):
            overrange (Variable):
            conversionFactor (Variable):
            config (Variable):
                enabled (Variable):
                gain (Variable):
                offset (Variable):
                alarms (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_LinearPositionMeasurementU32 (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            actual (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                millimeters (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                micrometers (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newMillimetersValue (Variable):
            average (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                millimeters (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                micrometers (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newMillimetersValue (Variable):
            rawValue (Variable):
            error (Variable):
            underrange (Variable):
            overrange (Variable):
            conversionFactor (Variable):
            config (Variable):
                enabled (Variable):
                gain (Variable):
                offset (Variable):
                alarms (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                alarmStatus (Variable):
                    superState (Variable):
                    config (Variable):
                        enabled (Variable):
                        loLo (Variable):
                        lo (Variable):
                        hi (Variable):
                        hiHi (Variable):
                    value (Variable):
                    disabled (Variable):
                    hiHi (Variable):
                    hi (Variable):
                    ok (Variable):
                    lo (Variable):
                    loLo (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        SM_SimpleRelay (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            digitalOutput (Variable):
            isEnabled (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
            processes (Variable):
                setEnabled (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    set (Variable):
                        enabled (Variable):
                    get (Variable):
                        enabled (Variable):
            setEnabled (Method):
                enabled (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        ChangeOperatingStateProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            set (Variable):
                state (Variable):
            get (Variable):
                state (Variable):
            start (Method):
                state (Variable):
            request (Method):
                state (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ChangeOperatorStateProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            set (Variable):
                state (Variable):
                password (Variable):
            get (Variable):
                state (Variable):
                password (Variable):
            start (Method):
                state (Variable):
                password (Variable):
            request (Method):
                state (Variable):
                password (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ChangeOperatingModeStateProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            set (Variable):
                state (Variable):
            get (Variable):
                state (Variable):
            start (Method):
                state (Variable):
            request (Method):
                state (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ReadXmlProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseXmlDataSrvProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                filePath (Variable):
            get (Variable):
                filePath (Variable):
            start (Method):
                filePath (Variable):
            request (Method):
                filePath (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        WriteXmlProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseXmlDataSrvProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                filePath (Variable):
            get (Variable):
                filePath (Variable):
            start (Method):
                filePath (Variable):
            request (Method):
                filePath (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ActivateProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            testVar (Variable):
            start (Method):
            request (Method):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SM_ConfigManager (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            filePath (Variable):
            isEnabled (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            processes (Variable):
                save (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        filePath (Variable):
                    get (Variable):
                        filePath (Variable):
                load (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        filePath (Variable):
                    get (Variable):
                        filePath (Variable):
                activate (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
            save (Method):
                filePath (Variable):
            load (Method):
                filePath (Variable):
            activate (Method):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
        ModbusRTUBusReadCoilProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            value (Variable):
            errorId (Variable):
            set (Variable):
                unitID (Variable):
                address (Variable):
            get (Variable):
                unitID (Variable):
                address (Variable):
            start (Method):
                unitID (Variable):
                address (Variable):
            request (Method):
                unitID (Variable):
                address (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ModbusRTUBusWriteCoilProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                unitID (Variable):
                address (Variable):
                value (Variable):
            get (Variable):
                unitID (Variable):
                address (Variable):
                value (Variable):
            start (Method):
                unitID (Variable):
                address (Variable):
                value (Variable):
            request (Method):
                unitID (Variable):
                address (Variable):
                value (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ModbusRTUBusReadRegisterProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            value (Variable):
            errorId (Variable):
            set (Variable):
                unitID (Variable):
                address (Variable):
            get (Variable):
                unitID (Variable):
                address (Variable):
            start (Method):
                unitID (Variable):
                address (Variable):
            request (Method):
                unitID (Variable):
                address (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ModbusRTUBusWriteRegisterProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            errorId (Variable):
            set (Variable):
                unitID (Variable):
                address (Variable):
                value (Variable):
            get (Variable):
                unitID (Variable):
                address (Variable):
                value (Variable):
            start (Method):
                unitID (Variable):
                address (Variable):
                value (Variable):
            request (Method):
                unitID (Variable):
                address (Variable):
                value (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        ModbusRTUBusDiagnosticsProcess (Process):
            SUPER (Pointer):
               points_to_type: BaseProcess
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
            value (Variable):
            errorId (Variable):
            set (Variable):
                unitID (Variable):
                subFunction (Variable):
                subData (Variable):
            get (Variable):
                unitID (Variable):
                subFunction (Variable):
                subData (Variable):
            start (Method):
                unitID (Variable):
                subFunction (Variable):
                subData (Variable):
            request (Method):
                unitID (Variable):
                subFunction (Variable):
                subData (Variable):
                ifthen (IfThen):
                    enabled (Variable):
            ifthen (IfThen):
                do_request (Variable):
        SM_ModbusRTUBus (Statemachine):
            actualStatus (Variable):
            previousStatus (Variable):
            retries (Variable):
            isEnabled (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
            processes (Variable):
                readCoil (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value (Variable):
                    errorId (Variable):
                    set (Variable):
                        unitID (Variable):
                        address (Variable):
                    get (Variable):
                        unitID (Variable):
                        address (Variable):
                writeCoil (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        unitID (Variable):
                        address (Variable):
                        value (Variable):
                    get (Variable):
                        unitID (Variable):
                        address (Variable):
                        value (Variable):
                readRegister (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value (Variable):
                    errorId (Variable):
                    set (Variable):
                        unitID (Variable):
                        address (Variable):
                    get (Variable):
                        unitID (Variable):
                        address (Variable):
                writeRegister (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    errorId (Variable):
                    set (Variable):
                        unitID (Variable):
                        address (Variable):
                        value (Variable):
                    get (Variable):
                        unitID (Variable):
                        address (Variable):
                        value (Variable):
                diagnostics (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value (Variable):
                    errorId (Variable):
                    set (Variable):
                        unitID (Variable):
                        subFunction (Variable):
                        subData (Variable):
                    get (Variable):
                        unitID (Variable):
                        subFunction (Variable):
                        subData (Variable):
            readCoil (Method):
                unitID (Variable):
                address (Variable):
            writeCoil (Method):
                unitID (Variable):
                address (Variable):
                value (Variable):
            readRegister (Method):
                unitID (Variable):
                address (Variable):
            writeRegister (Method):
                unitID (Variable):
                address (Variable):
                value (Variable):
            diagnostics (Method):
                unitID (Variable):
                subFunction (Variable):
                subData (Variable):
            _log (Method):
                name (Variable):
                buffer (Variable):
                subBuffer (Variable):
    InitializationStates (Enum):
        SHUTDOWN (EnumItem):
        INITIALIZING (EnumItem):
        INITIALIZING_FAILED (EnumItem):
        INITIALIZED (EnumItem):
        LOCKED (EnumItem):
    OperatorStates (Enum):
        NONE (EnumItem):
        OBSERVER (EnumItem):
        TECH (EnumItem):
    OperatingStates (Enum):
        AUTO (EnumItem):
        MANUAL (EnumItem):
    OperatingModeStates (Enum):
        LOCAL (EnumItem):
        REMOTE (EnumItem):
    LimitSwitches (Enum):
        POSITIVE_LIMIT_SWITCH (EnumItem):
        NEGATIVE_LIMIT_SWITCH (EnumItem):
    Units (Enum):
        DEGREE (EnumItem):
        RADIAN (EnumItem):
        PERCENT (EnumItem):
        RADIANS_PER_SECOND (EnumItem):
        DEGREES_PER_SECOND (EnumItem):
        MILLIMETERS_PER_SECOND (EnumItem):
        REVOLUTIONS_PER_SECOND (EnumItem):
        REVOLUTIONS_PER_MINUTE (EnumItem):
        AMPS (EnumItem):
        MILLIAMPS (EnumItem):
        MILLIMETER (EnumItem):
        MICROMETER (EnumItem):
        DEGREES_CELSIUS (EnumItem):
        KELVIN (EnumItem):
        BAR (EnumItem):
        PASCAL (EnumItem):
        UNITLESS (EnumItem):
        NEWTON (EnumItem):
        DECANEWTON (EnumItem):
        VOLT (EnumItem):
        MILLIVOLT (EnumItem):
        HERTZ (EnumItem):
        G (EnumItem):
        MILLIG (EnumItem):
        NEWTONMETER (EnumItem):
        ARCSECONDS (EnumItem):
        ARCSECONDS_PER_SECOND (EnumItem):
        RADIANS_PER_SQUARE_SECOND (EnumItem):
        DEGREES_PER_SQUARE_SECOND (EnumItem):
        ARCSECONDS_PER_SQUARE_SECOND (EnumItem):
        METERS_PER_SECOND (EnumItem):
        HECTOPASCAL (EnumItem):
        SECONDS (EnumItem):
        MINUTES (EnumItem):
        MILLIMETERS_PER_HOUR (EnumItem):
        HITS_PER_SQUARE_CENTIMETER (EnumItem):
        HITS_PER_SQUARE_CENTIMETER_PER_HOUR (EnumItem):
    RequestResults (Enum):
        ACCEPTED (EnumItem):
        REJECTED (EnumItem):
    ApertureStatus (Status):
        superState (Variable):
        isOpen (Variable):
        isClosed (Variable):
        open (Variable):
        closed (Variable):
        partiallyOpen (Variable):
        undefined (Variable):
    BusyStatus (Status):
        superState (Variable):
        isBusy (Variable):
        idle (Variable):
        busy (Variable):
    StartedStatus (Status):
        superState (Variable):
        isStarted (Variable):
        started (Variable):
        notStarted (Variable):
    EnabledStatus (Status):
        superState (Variable):
        isEnabled (Variable):
        enabled (Variable):
        disabled (Variable):
    PoweredStatus (Status):
        superState (Variable):
        isEnabled (Variable):
        enabled (Variable):
        disabled (Variable):
    HealthStatus (Status):
        superState (Variable):
        isGood (Variable):
        hasWarning (Variable):
        good (Variable):
        warning (Variable):
        bad (Variable):
    InitializationStatus (Status):
        superState (Variable):
        state (Variable):
        shutdown (Variable):
        initializing (Variable):
        initializingFailed (Variable):
        initialized (Variable):
        locked (Variable):
    MotionStatus (Status):
        superState (Variable):
        actVel (Variable):
        tolerance (Variable):
        forward (Variable):
        backward (Variable):
        standstill (Variable):
    HiHiLoLoAlarmConfig (Config):
        enabled (Variable):
        loLo (Variable):
        lo (Variable):
        hi (Variable):
        hiHi (Variable):
    HiHiLoLoAlarmStatus (Status):
        superState (Variable):
        config (Variable):
            enabled (Variable):
            loLo (Variable):
            lo (Variable):
            hi (Variable):
            hiHi (Variable):
        value (Variable):
        disabled (Variable):
        hiHi (Variable):
        hi (Variable):
        ok (Variable):
        lo (Variable):
        loLo (Variable):
    MeasurementConfig (Config):
        enabled (Variable):
        gain (Variable):
        offset (Variable):
        alarms (Variable):
            enabled (Variable):
            loLo (Variable):
            lo (Variable):
            hi (Variable):
            hiHi (Variable):
    OpeningStatus (Status):
        superState (Variable):
        isOpening (Variable):
        isClosing (Variable):
        opening (Variable):
        closing (Variable):
        standstill (Variable):
    OperatorStatus (Status):
        superState (Variable):
        state (Variable):
        none (Variable):
        tech (Variable):
        observer (Variable):
    OperatingStatus (Status):
        superState (Variable):
        state (Variable):
        auto (Variable):
        manual (Variable):
    OperatingModeStatus (Status):
        superState (Variable):
        state (Variable):
        local (Variable):
        remote (Variable):
    ActivityStatus (Status):
        superState (Variable):
        isMoving (Variable):
        isAwake (Variable):
        moving (Variable):
        awake (Variable):
        sleeping (Variable):
    SM_QuantityValue (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        value (Variable):
        unit (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    QuantityValue (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_QuantityValue
        actualStatus (Variable):
        previousStatus (Variable):
        value (Variable):
        unit (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_AngularPosition (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        radians (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        degrees (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        arcseconds (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newDegreesValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    AngularPosition (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_AngularPosition
        actualStatus (Variable):
        previousStatus (Variable):
        radians (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        degrees (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        arcseconds (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newDegreesValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_LinearPosition (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        millimeters (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        micrometers (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newMillimetersValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    LinearPosition (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_LinearPosition
        actualStatus (Variable):
        previousStatus (Variable):
        millimeters (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        micrometers (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newMillimetersValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_AngularVelocity (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        radiansPerSecond (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        degreesPerSecond (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        arcsecondsPerSecond (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newDegreesPerSecondValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    AngularVelocity (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_AngularVelocity
        actualStatus (Variable):
        previousStatus (Variable):
        radiansPerSecond (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        degreesPerSecond (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        arcsecondsPerSecond (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newDegreesPerSecondValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_AngularAcceleration (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        radiansPerSquareSecond (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        degreesPerSquareSecond (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        arcsecondsPerSquareSecond (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newDegreesPerSquareSecondValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    AngularAcceleration (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_AngularAcceleration
        actualStatus (Variable):
        previousStatus (Variable):
        radiansPerSquareSecond (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        degreesPerSquareSecond (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        arcsecondsPerSquareSecond (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newDegreesPerSquareSecondValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_LinearVelocity (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        millimetersPerSecond (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newMillimetersPerSecondValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    LinearVelocity (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_LinearVelocity
        actualStatus (Variable):
        previousStatus (Variable):
        millimetersPerSecond (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newMillimetersPerSecondValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_Voltage (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        volt (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        milliVolt (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newVoltValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    Voltage (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_Voltage
        actualStatus (Variable):
        previousStatus (Variable):
        volt (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        milliVolt (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newVoltValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_GForce (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        g (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        milliG (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newMilliGValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    GForce (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_GForce
        actualStatus (Variable):
        previousStatus (Variable):
        g (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        milliG (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newMilliGValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_Current (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        amps (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        milliAmps (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newAmpsValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    Current (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_Current
        actualStatus (Variable):
        previousStatus (Variable):
        amps (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        milliAmps (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newAmpsValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_Torque (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        newtonmeter (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newNewtonmeterValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    Torque (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_Torque
        actualStatus (Variable):
        previousStatus (Variable):
        newtonmeter (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newNewtonmeterValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_TorqueLimit (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        unitless (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        percent (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newtonmeter (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newFractionValue (Variable):
        maxNewtonmeter (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    TorqueLimit (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_TorqueLimit
        actualStatus (Variable):
        previousStatus (Variable):
        unitless (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        percent (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newtonmeter (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newFractionValue (Variable):
        maxNewtonmeter (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_AverageCurrent (Statemachine):
        SUPER (Pointer):
           points_to_type: Current
        actualStatus (Variable):
        previousStatus (Variable):
        amps (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        milliAmps (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newAmpsValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    AverageCurrent (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_AverageCurrent
        actualStatus (Variable):
        previousStatus (Variable):
        amps (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        milliAmps (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newAmpsValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_Duration (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        seconds (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        minutes (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newSecondsValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    Duration (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_Duration
        actualStatus (Variable):
        previousStatus (Variable):
        seconds (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        minutes (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newSecondsValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_Temperature (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        celsius (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        kelvin (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newCelsiusValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    Temperature (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_Temperature
        actualStatus (Variable):
        previousStatus (Variable):
        celsius (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        kelvin (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newCelsiusValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_Pressure (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        bar (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        pascal (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        hectoPascal (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newBarValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    Pressure (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_Pressure
        actualStatus (Variable):
        previousStatus (Variable):
        bar (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        pascal (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        hectoPascal (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newBarValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_Frequency (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        hertz (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newHertzValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    Frequency (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_Frequency
        actualStatus (Variable):
        previousStatus (Variable):
        hertz (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newHertzValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_Force (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        newton (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        decaNewton (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newNewtonValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    Force (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_Force
        actualStatus (Variable):
        previousStatus (Variable):
        newton (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        decaNewton (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newNewtonValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_Fraction (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        unitless (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        percent (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newFractionValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    Fraction (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_Fraction
        actualStatus (Variable):
        previousStatus (Variable):
        unitless (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        percent (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            value (Variable):
            unit (Variable):
        newFractionValue (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_BaseProcess (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    BaseProcessStatuses (Struct):
        enabledStatus (Variable):
            superState (Variable):
            isEnabled (Variable):
            enabled (Variable):
            disabled (Variable):
        busyStatus (Variable):
            superState (Variable):
            isBusy (Variable):
            idle (Variable):
            busy (Variable):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
        startedStatus (Variable):
            superState (Variable):
            isStarted (Variable):
            started (Variable):
            notStarted (Variable):
    BaseProcess (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_BaseMcProcess (Statemachine):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
    BaseMcProcess (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_BaseMcProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
    SM_ProcessStep (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        process (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    ProcessStepStatuses (Struct):
        enabledStatus (Variable):
            superState (Variable):
            isEnabled (Variable):
            enabled (Variable):
            disabled (Variable):
    ProcessStep (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_ProcessStep
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        process (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_BaseXmlDataSrvProcess (Statemachine):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
    BaseXmlDataSrvProcess (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_BaseXmlDataSrvProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
    Process (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        testVar (Variable):
        start (Method):
        request (Method):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    McProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseMcProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        testVar (Variable):
        start (Method):
        request (Method):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    ChangeSetpointProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        set (Variable):
            setpoint (Variable):
        get (Variable):
            setpoint (Variable):
        start (Method):
            setpoint (Variable):
        request (Method):
            setpoint (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    ChangeSetpointProcessArgs (Struct):
        setpoint (Variable):
    ChangeParameterProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        set (Variable):
            parameter (Variable):
        get (Variable):
            parameter (Variable):
        start (Method):
            parameter (Variable):
        request (Method):
            parameter (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    ChangeParameterProcessArgs (Struct):
        parameter (Variable):
    McMoveAbsoluteProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseMcProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        set (Variable):
            position (Variable):
            maxVelocity (Variable):
        get (Variable):
            position (Variable):
            maxVelocity (Variable):
        start (Method):
            position (Variable):
            maxVelocity (Variable):
        request (Method):
            position (Variable):
            maxVelocity (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    McMoveAbsoluteProcessArgs (Struct):
        position (Variable):
        maxVelocity (Variable):
    McMoveRelativeProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseMcProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        set (Variable):
            distance (Variable):
            maxVelocity (Variable):
        get (Variable):
            distance (Variable):
            maxVelocity (Variable):
        start (Method):
            distance (Variable):
            maxVelocity (Variable):
        request (Method):
            distance (Variable):
            maxVelocity (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    McMoveRelativeProcessArgs (Struct):
        distance (Variable):
        maxVelocity (Variable):
    McPowerProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseMcProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        set (Variable):
            enable (Variable):
        get (Variable):
            enable (Variable):
        start (Method):
            enable (Variable):
        request (Method):
            enable (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    McPowerProcessArgs (Struct):
        enable (Variable):
    McSetPositionProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseMcProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        set (Variable):
            value (Variable):
        get (Variable):
            value (Variable):
        start (Method):
            value (Variable):
        request (Method):
            value (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    McSetPositionProcessArgs (Struct):
        value (Variable):
    McStopProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseMcProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        set (Variable):
            deceleration (Variable):
            jerk (Variable):
        get (Variable):
            deceleration (Variable):
            jerk (Variable):
        start (Method):
            deceleration (Variable):
            jerk (Variable):
        request (Method):
            deceleration (Variable):
            jerk (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    McStopProcessArgs (Struct):
        deceleration (Variable):
        jerk (Variable):
    McMoveVelocityProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseMcProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        set (Variable):
            value (Variable):
        get (Variable):
            value (Variable):
        start (Method):
            value (Variable):
        request (Method):
            value (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    McMoveVelocityProcessArgs (Struct):
        value (Variable):
    SetEnabledProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        set (Variable):
            enabled (Variable):
        get (Variable):
            enabled (Variable):
        start (Method):
            enabled (Variable):
        request (Method):
            enabled (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    SetEnabledProcessArgs (Struct):
        enabled (Variable):
    McWriteParameter (Process):
        SUPER (Pointer):
           points_to_type: BaseMcProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        set (Variable):
            parameterNumber (Variable):
            value (Variable):
        get (Variable):
            parameterNumber (Variable):
            value (Variable):
        start (Method):
            parameterNumber (Variable):
            value (Variable):
        request (Method):
            parameterNumber (Variable):
            value (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    McWriteParameterArgs (Struct):
        parameterNumber (Variable):
        value (Variable):
    McReadParameter (Process):
        SUPER (Pointer):
           points_to_type: BaseMcProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        value (Variable):
        set (Variable):
            parameterNumber (Variable):
        get (Variable):
            parameterNumber (Variable):
        start (Method):
            parameterNumber (Variable):
        request (Method):
            parameterNumber (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    McReadParameterArgs (Struct):
        parameterNumber (Variable):
    McWriteBoolParameter (Process):
        SUPER (Pointer):
           points_to_type: BaseMcProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        set (Variable):
            parameterNumber (Variable):
            value (Variable):
        get (Variable):
            parameterNumber (Variable):
            value (Variable):
        start (Method):
            parameterNumber (Variable):
            value (Variable):
        request (Method):
            parameterNumber (Variable):
            value (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    McWriteBoolParameterArgs (Struct):
        parameterNumber (Variable):
        value (Variable):
    McReadBoolParameter (Process):
        SUPER (Pointer):
           points_to_type: BaseMcProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        value (Variable):
        set (Variable):
            parameterNumber (Variable):
        get (Variable):
            parameterNumber (Variable):
        start (Method):
            parameterNumber (Variable):
        request (Method):
            parameterNumber (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    McReadBoolParameterArgs (Struct):
        parameterNumber (Variable):
    McProbe (Process):
        SUPER (Pointer):
           points_to_type: BaseMcProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        recordedPosition (Variable):
        set (Variable):
            risingEdge (Variable):
            timeout (Variable):
        get (Variable):
            risingEdge (Variable):
            timeout (Variable):
        start (Method):
            risingEdge (Variable):
            timeout (Variable):
        request (Method):
            risingEdge (Variable):
            timeout (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    McProbeArgs (Struct):
        risingEdge (Variable):
        timeout (Variable):
    SM_BaseAxis (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        axis_ref (Variable):
            PlcToNc (Variable):
                ControlDWord (Variable):
                Override (Variable):
                AxisModeRequest (Variable):
                AxisModeDWord (Variable):
                AxisModeLReal (Variable):
                PositionCorrection (Variable):
                ExtSetPos (Variable):
                ExtSetVelo (Variable):
                ExtSetAcc (Variable):
                ExtSetDirection (Variable):
                ExtControllerOutput (Variable):
                GearRatio1 (Variable):
                GearRatio2 (Variable):
                GearRatio3 (Variable):
                GearRatio4 (Variable):
            NcToPlc (Variable):
                StateDWord (Variable):
                ErrorCode (Variable):
                AxisState (Variable):
                AxisModeConfirmation (Variable):
                HomingState (Variable):
                CoupleState (Variable):
                SvbEntries (Variable):
                SafEntries (Variable):
                AxisId (Variable):
                OpModeDWord (Variable):
                ActiveControlLoopIndex (Variable):
                ControlLoopIndex (Variable):
                ActPos (Variable):
                ModuloActPos (Variable):
                ModuloActTurns (Variable):
                ActVelo (Variable):
                PosDiff (Variable):
                SetPos (Variable):
                SetVelo (Variable):
                SetAcc (Variable):
                TargetPos (Variable):
                ModuloSetPos (Variable):
                ModuloSetTurns (Variable):
                CmdNo (Variable):
                CmdState (Variable):
            Status (Variable):
                UpdateTaskIndex (Variable):
                UpdateCycleTime (Variable):
                CycleCounter (Variable):
                NcCycleCounter (Variable):
                Error (Variable):
                ErrorId (Variable):
                Disabled (Variable):
                ControlLoopClosed (Variable):
                ExtSetPointGenEnabled (Variable):
        isGearingSupported (Variable):
        isJogEnabled (Variable):
        isEnabled (Variable):
        standstillTolerance (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            poweredStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            extSetGenStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            motionStatus (Variable):
                superState (Variable):
                actVel (Variable):
                tolerance (Variable):
                forward (Variable):
                backward (Variable):
                standstill (Variable):
        processes (Variable):
            moveAbsolute (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    position (Variable):
                    maxVelocity (Variable):
                get (Variable):
                    position (Variable):
                    maxVelocity (Variable):
            moveRelative (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    distance (Variable):
                    maxVelocity (Variable):
                get (Variable):
                    distance (Variable):
                    maxVelocity (Variable):
            moveVelocity (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    value (Variable):
                get (Variable):
                    value (Variable):
            reset (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            stop (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            stopParametrized (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    deceleration (Variable):
                    jerk (Variable):
                get (Variable):
                    deceleration (Variable):
                    jerk (Variable):
            power (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    enable (Variable):
                get (Variable):
                    enable (Variable):
            gearIn (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            gearOut (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            initialize (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
            setPosition (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    value (Variable):
                get (Variable):
                    value (Variable):
            enableExtSetGen (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            disableExtSetGen (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            forceCalibration (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            resetCalibration (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            readParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                value (Variable):
                set (Variable):
                    parameterNumber (Variable):
                get (Variable):
                    parameterNumber (Variable):
            readBoolParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                value (Variable):
                set (Variable):
                    parameterNumber (Variable):
                get (Variable):
                    parameterNumber (Variable):
            writeParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    parameterNumber (Variable):
                    value (Variable):
                get (Variable):
                    parameterNumber (Variable):
                    value (Variable):
            writeBoolParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    parameterNumber (Variable):
                    value (Variable):
                get (Variable):
                    parameterNumber (Variable):
                    value (Variable):
            probeStart (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                recordedPosition (Variable):
                set (Variable):
                    risingEdge (Variable):
                    timeout (Variable):
                get (Variable):
                    risingEdge (Variable):
                    timeout (Variable):
            probeAbort (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
        moveAbsolute (Method):
            position (Variable):
            maxVelocity (Variable):
        moveRelative (Method):
            distance (Variable):
            maxVelocity (Variable):
        moveVelocity (Method):
            value (Variable):
        reset (Method):
        stop (Method):
        stopParametrized (Method):
            deceleration (Variable):
            jerk (Variable):
        power (Method):
            enable (Variable):
        gearIn (Method):
        gearOut (Method):
        initialize (Method):
        setPosition (Method):
            value (Variable):
        enableExtSetGen (Method):
        disableExtSetGen (Method):
        forceCalibration (Method):
        resetCalibration (Method):
        readParameter (Method):
            parameterNumber (Variable):
        readBoolParameter (Method):
            parameterNumber (Variable):
        writeParameter (Method):
            parameterNumber (Variable):
            value (Variable):
        writeBoolParameter (Method):
            parameterNumber (Variable):
            value (Variable):
        probeStart (Method):
            risingEdge (Variable):
            timeout (Variable):
        probeAbort (Method):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    BaseAxisStatuses (Struct):
        busyStatus (Variable):
            superState (Variable):
            isBusy (Variable):
            idle (Variable):
            busy (Variable):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
        poweredStatus (Variable):
            superState (Variable):
            isEnabled (Variable):
            enabled (Variable):
            disabled (Variable):
        extSetGenStatus (Variable):
            superState (Variable):
            isEnabled (Variable):
            enabled (Variable):
            disabled (Variable):
        motionStatus (Variable):
            superState (Variable):
            actVel (Variable):
            tolerance (Variable):
            forward (Variable):
            backward (Variable):
            standstill (Variable):
    BaseAxisProcesses (Struct):
        moveAbsolute (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            set (Variable):
                position (Variable):
                maxVelocity (Variable):
            get (Variable):
                position (Variable):
                maxVelocity (Variable):
        moveRelative (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            set (Variable):
                distance (Variable):
                maxVelocity (Variable):
            get (Variable):
                distance (Variable):
                maxVelocity (Variable):
        moveVelocity (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            set (Variable):
                value (Variable):
            get (Variable):
                value (Variable):
        reset (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            testVar (Variable):
        stop (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            testVar (Variable):
        stopParametrized (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            set (Variable):
                deceleration (Variable):
                jerk (Variable):
            get (Variable):
                deceleration (Variable):
                jerk (Variable):
        power (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            set (Variable):
                enable (Variable):
            get (Variable):
                enable (Variable):
        gearIn (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            testVar (Variable):
        gearOut (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            testVar (Variable):
        initialize (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            testVar (Variable):
        setPosition (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            set (Variable):
                value (Variable):
            get (Variable):
                value (Variable):
        enableExtSetGen (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            testVar (Variable):
        disableExtSetGen (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            testVar (Variable):
        forceCalibration (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            testVar (Variable):
        resetCalibration (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            testVar (Variable):
        readParameter (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            value (Variable):
            set (Variable):
                parameterNumber (Variable):
            get (Variable):
                parameterNumber (Variable):
        readBoolParameter (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            value (Variable):
            set (Variable):
                parameterNumber (Variable):
            get (Variable):
                parameterNumber (Variable):
        writeParameter (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            set (Variable):
                parameterNumber (Variable):
                value (Variable):
            get (Variable):
                parameterNumber (Variable):
                value (Variable):
        writeBoolParameter (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            set (Variable):
                parameterNumber (Variable):
                value (Variable):
            get (Variable):
                parameterNumber (Variable):
                value (Variable):
        probeStart (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            recordedPosition (Variable):
            set (Variable):
                risingEdge (Variable):
                timeout (Variable):
            get (Variable):
                risingEdge (Variable):
                timeout (Variable):
        probeAbort (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            testVar (Variable):
    BaseAxis (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_BaseAxis
        actualStatus (Variable):
        previousStatus (Variable):
        axis_ref (Variable):
            PlcToNc (Variable):
                ControlDWord (Variable):
                Override (Variable):
                AxisModeRequest (Variable):
                AxisModeDWord (Variable):
                AxisModeLReal (Variable):
                PositionCorrection (Variable):
                ExtSetPos (Variable):
                ExtSetVelo (Variable):
                ExtSetAcc (Variable):
                ExtSetDirection (Variable):
                ExtControllerOutput (Variable):
                GearRatio1 (Variable):
                GearRatio2 (Variable):
                GearRatio3 (Variable):
                GearRatio4 (Variable):
            NcToPlc (Variable):
                StateDWord (Variable):
                ErrorCode (Variable):
                AxisState (Variable):
                AxisModeConfirmation (Variable):
                HomingState (Variable):
                CoupleState (Variable):
                SvbEntries (Variable):
                SafEntries (Variable):
                AxisId (Variable):
                OpModeDWord (Variable):
                ActiveControlLoopIndex (Variable):
                ControlLoopIndex (Variable):
                ActPos (Variable):
                ModuloActPos (Variable):
                ModuloActTurns (Variable):
                ActVelo (Variable):
                PosDiff (Variable):
                SetPos (Variable):
                SetVelo (Variable):
                SetAcc (Variable):
                TargetPos (Variable):
                ModuloSetPos (Variable):
                ModuloSetTurns (Variable):
                CmdNo (Variable):
                CmdState (Variable):
            Status (Variable):
                UpdateTaskIndex (Variable):
                UpdateCycleTime (Variable):
                CycleCounter (Variable):
                NcCycleCounter (Variable):
                Error (Variable):
                ErrorId (Variable):
                Disabled (Variable):
                ControlLoopClosed (Variable):
                ExtSetPointGenEnabled (Variable):
        isGearingSupported (Variable):
        isJogEnabled (Variable):
        isEnabled (Variable):
        standstillTolerance (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            poweredStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            extSetGenStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            motionStatus (Variable):
                superState (Variable):
                actVel (Variable):
                tolerance (Variable):
                forward (Variable):
                backward (Variable):
                standstill (Variable):
        processes (Variable):
            moveAbsolute (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    position (Variable):
                    maxVelocity (Variable):
                get (Variable):
                    position (Variable):
                    maxVelocity (Variable):
            moveRelative (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    distance (Variable):
                    maxVelocity (Variable):
                get (Variable):
                    distance (Variable):
                    maxVelocity (Variable):
            moveVelocity (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    value (Variable):
                get (Variable):
                    value (Variable):
            reset (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            stop (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            stopParametrized (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    deceleration (Variable):
                    jerk (Variable):
                get (Variable):
                    deceleration (Variable):
                    jerk (Variable):
            power (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    enable (Variable):
                get (Variable):
                    enable (Variable):
            gearIn (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            gearOut (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            initialize (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
            setPosition (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    value (Variable):
                get (Variable):
                    value (Variable):
            enableExtSetGen (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            disableExtSetGen (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            forceCalibration (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            resetCalibration (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            readParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                value (Variable):
                set (Variable):
                    parameterNumber (Variable):
                get (Variable):
                    parameterNumber (Variable):
            readBoolParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                value (Variable):
                set (Variable):
                    parameterNumber (Variable):
                get (Variable):
                    parameterNumber (Variable):
            writeParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    parameterNumber (Variable):
                    value (Variable):
                get (Variable):
                    parameterNumber (Variable):
                    value (Variable):
            writeBoolParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    parameterNumber (Variable):
                    value (Variable):
                get (Variable):
                    parameterNumber (Variable):
                    value (Variable):
            probeStart (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                recordedPosition (Variable):
                set (Variable):
                    risingEdge (Variable):
                    timeout (Variable):
                get (Variable):
                    risingEdge (Variable):
                    timeout (Variable):
            probeAbort (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
        moveAbsolute (Method):
            position (Variable):
            maxVelocity (Variable):
        moveRelative (Method):
            distance (Variable):
            maxVelocity (Variable):
        moveVelocity (Method):
            value (Variable):
        reset (Method):
        stop (Method):
        stopParametrized (Method):
            deceleration (Variable):
            jerk (Variable):
        power (Method):
            enable (Variable):
        gearIn (Method):
        gearOut (Method):
        initialize (Method):
        setPosition (Method):
            value (Variable):
        enableExtSetGen (Method):
        disableExtSetGen (Method):
        forceCalibration (Method):
        resetCalibration (Method):
        readParameter (Method):
            parameterNumber (Variable):
        readBoolParameter (Method):
            parameterNumber (Variable):
        writeParameter (Method):
            parameterNumber (Variable):
            value (Variable):
        writeBoolParameter (Method):
            parameterNumber (Variable):
            value (Variable):
        probeStart (Method):
            risingEdge (Variable):
            timeout (Variable):
        probeAbort (Method):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_AngularAxis (Statemachine):
        SUPER (Pointer):
           points_to_type: BaseAxis
        actualStatus (Variable):
        previousStatus (Variable):
        axis_ref (Variable):
            PlcToNc (Variable):
                ControlDWord (Variable):
                Override (Variable):
                AxisModeRequest (Variable):
                AxisModeDWord (Variable):
                AxisModeLReal (Variable):
                PositionCorrection (Variable):
                ExtSetPos (Variable):
                ExtSetVelo (Variable):
                ExtSetAcc (Variable):
                ExtSetDirection (Variable):
                ExtControllerOutput (Variable):
                GearRatio1 (Variable):
                GearRatio2 (Variable):
                GearRatio3 (Variable):
                GearRatio4 (Variable):
            NcToPlc (Variable):
                StateDWord (Variable):
                ErrorCode (Variable):
                AxisState (Variable):
                AxisModeConfirmation (Variable):
                HomingState (Variable):
                CoupleState (Variable):
                SvbEntries (Variable):
                SafEntries (Variable):
                AxisId (Variable):
                OpModeDWord (Variable):
                ActiveControlLoopIndex (Variable):
                ControlLoopIndex (Variable):
                ActPos (Variable):
                ModuloActPos (Variable):
                ModuloActTurns (Variable):
                ActVelo (Variable):
                PosDiff (Variable):
                SetPos (Variable):
                SetVelo (Variable):
                SetAcc (Variable):
                TargetPos (Variable):
                ModuloSetPos (Variable):
                ModuloSetTurns (Variable):
                CmdNo (Variable):
                CmdState (Variable):
            Status (Variable):
                UpdateTaskIndex (Variable):
                UpdateCycleTime (Variable):
                CycleCounter (Variable):
                NcCycleCounter (Variable):
                Error (Variable):
                ErrorId (Variable):
                Disabled (Variable):
                ControlLoopClosed (Variable):
                ExtSetPointGenEnabled (Variable):
        isGearingSupported (Variable):
        isJogEnabled (Variable):
        isEnabled (Variable):
        standstillTolerance (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            poweredStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            extSetGenStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            motionStatus (Variable):
                superState (Variable):
                actVel (Variable):
                tolerance (Variable):
                forward (Variable):
                backward (Variable):
                standstill (Variable):
        processes (Variable):
            moveAbsolute (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    position (Variable):
                    maxVelocity (Variable):
                get (Variable):
                    position (Variable):
                    maxVelocity (Variable):
            moveRelative (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    distance (Variable):
                    maxVelocity (Variable):
                get (Variable):
                    distance (Variable):
                    maxVelocity (Variable):
            moveVelocity (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    value (Variable):
                get (Variable):
                    value (Variable):
            reset (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            stop (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            stopParametrized (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    deceleration (Variable):
                    jerk (Variable):
                get (Variable):
                    deceleration (Variable):
                    jerk (Variable):
            power (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    enable (Variable):
                get (Variable):
                    enable (Variable):
            gearIn (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            gearOut (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            initialize (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
            setPosition (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    value (Variable):
                get (Variable):
                    value (Variable):
            enableExtSetGen (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            disableExtSetGen (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            forceCalibration (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            resetCalibration (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            readParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                value (Variable):
                set (Variable):
                    parameterNumber (Variable):
                get (Variable):
                    parameterNumber (Variable):
            readBoolParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                value (Variable):
                set (Variable):
                    parameterNumber (Variable):
                get (Variable):
                    parameterNumber (Variable):
            writeParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    parameterNumber (Variable):
                    value (Variable):
                get (Variable):
                    parameterNumber (Variable):
                    value (Variable):
            writeBoolParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    parameterNumber (Variable):
                    value (Variable):
                get (Variable):
                    parameterNumber (Variable):
                    value (Variable):
            probeStart (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                recordedPosition (Variable):
                set (Variable):
                    risingEdge (Variable):
                    timeout (Variable):
                get (Variable):
                    risingEdge (Variable):
                    timeout (Variable):
            probeAbort (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
        moveAbsolute (Method):
            position (Variable):
            maxVelocity (Variable):
        moveRelative (Method):
            distance (Variable):
            maxVelocity (Variable):
        moveVelocity (Method):
            value (Variable):
        reset (Method):
        stop (Method):
        stopParametrized (Method):
            deceleration (Variable):
            jerk (Variable):
        power (Method):
            enable (Variable):
        gearIn (Method):
        gearOut (Method):
        initialize (Method):
        setPosition (Method):
            value (Variable):
        enableExtSetGen (Method):
        disableExtSetGen (Method):
        forceCalibration (Method):
        resetCalibration (Method):
        readParameter (Method):
            parameterNumber (Variable):
        readBoolParameter (Method):
            parameterNumber (Variable):
        writeParameter (Method):
            parameterNumber (Variable):
            value (Variable):
        writeBoolParameter (Method):
            parameterNumber (Variable):
            value (Variable):
        probeStart (Method):
            risingEdge (Variable):
            timeout (Variable):
        probeAbort (Method):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        actPos (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            radians (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            degrees (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            arcseconds (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newDegreesValue (Variable):
        actVel (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            radiansPerSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            degreesPerSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            arcsecondsPerSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newDegreesPerSecondValue (Variable):
    AngularAxis (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_AngularAxis
        actualStatus (Variable):
        previousStatus (Variable):
        axis_ref (Variable):
            PlcToNc (Variable):
                ControlDWord (Variable):
                Override (Variable):
                AxisModeRequest (Variable):
                AxisModeDWord (Variable):
                AxisModeLReal (Variable):
                PositionCorrection (Variable):
                ExtSetPos (Variable):
                ExtSetVelo (Variable):
                ExtSetAcc (Variable):
                ExtSetDirection (Variable):
                ExtControllerOutput (Variable):
                GearRatio1 (Variable):
                GearRatio2 (Variable):
                GearRatio3 (Variable):
                GearRatio4 (Variable):
            NcToPlc (Variable):
                StateDWord (Variable):
                ErrorCode (Variable):
                AxisState (Variable):
                AxisModeConfirmation (Variable):
                HomingState (Variable):
                CoupleState (Variable):
                SvbEntries (Variable):
                SafEntries (Variable):
                AxisId (Variable):
                OpModeDWord (Variable):
                ActiveControlLoopIndex (Variable):
                ControlLoopIndex (Variable):
                ActPos (Variable):
                ModuloActPos (Variable):
                ModuloActTurns (Variable):
                ActVelo (Variable):
                PosDiff (Variable):
                SetPos (Variable):
                SetVelo (Variable):
                SetAcc (Variable):
                TargetPos (Variable):
                ModuloSetPos (Variable):
                ModuloSetTurns (Variable):
                CmdNo (Variable):
                CmdState (Variable):
            Status (Variable):
                UpdateTaskIndex (Variable):
                UpdateCycleTime (Variable):
                CycleCounter (Variable):
                NcCycleCounter (Variable):
                Error (Variable):
                ErrorId (Variable):
                Disabled (Variable):
                ControlLoopClosed (Variable):
                ExtSetPointGenEnabled (Variable):
        isGearingSupported (Variable):
        isJogEnabled (Variable):
        isEnabled (Variable):
        standstillTolerance (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            poweredStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            extSetGenStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            motionStatus (Variable):
                superState (Variable):
                actVel (Variable):
                tolerance (Variable):
                forward (Variable):
                backward (Variable):
                standstill (Variable):
        processes (Variable):
            moveAbsolute (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    position (Variable):
                    maxVelocity (Variable):
                get (Variable):
                    position (Variable):
                    maxVelocity (Variable):
            moveRelative (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    distance (Variable):
                    maxVelocity (Variable):
                get (Variable):
                    distance (Variable):
                    maxVelocity (Variable):
            moveVelocity (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    value (Variable):
                get (Variable):
                    value (Variable):
            reset (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            stop (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            stopParametrized (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    deceleration (Variable):
                    jerk (Variable):
                get (Variable):
                    deceleration (Variable):
                    jerk (Variable):
            power (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    enable (Variable):
                get (Variable):
                    enable (Variable):
            gearIn (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            gearOut (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            initialize (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
            setPosition (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    value (Variable):
                get (Variable):
                    value (Variable):
            enableExtSetGen (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            disableExtSetGen (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            forceCalibration (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            resetCalibration (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            readParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                value (Variable):
                set (Variable):
                    parameterNumber (Variable):
                get (Variable):
                    parameterNumber (Variable):
            readBoolParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                value (Variable):
                set (Variable):
                    parameterNumber (Variable):
                get (Variable):
                    parameterNumber (Variable):
            writeParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    parameterNumber (Variable):
                    value (Variable):
                get (Variable):
                    parameterNumber (Variable):
                    value (Variable):
            writeBoolParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    parameterNumber (Variable):
                    value (Variable):
                get (Variable):
                    parameterNumber (Variable):
                    value (Variable):
            probeStart (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                recordedPosition (Variable):
                set (Variable):
                    risingEdge (Variable):
                    timeout (Variable):
                get (Variable):
                    risingEdge (Variable):
                    timeout (Variable):
            probeAbort (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
        moveAbsolute (Method):
            position (Variable):
            maxVelocity (Variable):
        moveRelative (Method):
            distance (Variable):
            maxVelocity (Variable):
        moveVelocity (Method):
            value (Variable):
        reset (Method):
        stop (Method):
        stopParametrized (Method):
            deceleration (Variable):
            jerk (Variable):
        power (Method):
            enable (Variable):
        gearIn (Method):
        gearOut (Method):
        initialize (Method):
        setPosition (Method):
            value (Variable):
        enableExtSetGen (Method):
        disableExtSetGen (Method):
        forceCalibration (Method):
        resetCalibration (Method):
        readParameter (Method):
            parameterNumber (Variable):
        readBoolParameter (Method):
            parameterNumber (Variable):
        writeParameter (Method):
            parameterNumber (Variable):
            value (Variable):
        writeBoolParameter (Method):
            parameterNumber (Variable):
            value (Variable):
        probeStart (Method):
            risingEdge (Variable):
            timeout (Variable):
        probeAbort (Method):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        actPos (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            radians (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            degrees (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            arcseconds (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newDegreesValue (Variable):
        actVel (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            radiansPerSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            degreesPerSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            arcsecondsPerSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newDegreesPerSecondValue (Variable):
    SM_LinearAxis (Statemachine):
        SUPER (Pointer):
           points_to_type: BaseAxis
        actualStatus (Variable):
        previousStatus (Variable):
        axis_ref (Variable):
            PlcToNc (Variable):
                ControlDWord (Variable):
                Override (Variable):
                AxisModeRequest (Variable):
                AxisModeDWord (Variable):
                AxisModeLReal (Variable):
                PositionCorrection (Variable):
                ExtSetPos (Variable):
                ExtSetVelo (Variable):
                ExtSetAcc (Variable):
                ExtSetDirection (Variable):
                ExtControllerOutput (Variable):
                GearRatio1 (Variable):
                GearRatio2 (Variable):
                GearRatio3 (Variable):
                GearRatio4 (Variable):
            NcToPlc (Variable):
                StateDWord (Variable):
                ErrorCode (Variable):
                AxisState (Variable):
                AxisModeConfirmation (Variable):
                HomingState (Variable):
                CoupleState (Variable):
                SvbEntries (Variable):
                SafEntries (Variable):
                AxisId (Variable):
                OpModeDWord (Variable):
                ActiveControlLoopIndex (Variable):
                ControlLoopIndex (Variable):
                ActPos (Variable):
                ModuloActPos (Variable):
                ModuloActTurns (Variable):
                ActVelo (Variable):
                PosDiff (Variable):
                SetPos (Variable):
                SetVelo (Variable):
                SetAcc (Variable):
                TargetPos (Variable):
                ModuloSetPos (Variable):
                ModuloSetTurns (Variable):
                CmdNo (Variable):
                CmdState (Variable):
            Status (Variable):
                UpdateTaskIndex (Variable):
                UpdateCycleTime (Variable):
                CycleCounter (Variable):
                NcCycleCounter (Variable):
                Error (Variable):
                ErrorId (Variable):
                Disabled (Variable):
                ControlLoopClosed (Variable):
                ExtSetPointGenEnabled (Variable):
        isGearingSupported (Variable):
        isJogEnabled (Variable):
        isEnabled (Variable):
        standstillTolerance (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            poweredStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            extSetGenStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            motionStatus (Variable):
                superState (Variable):
                actVel (Variable):
                tolerance (Variable):
                forward (Variable):
                backward (Variable):
                standstill (Variable):
        processes (Variable):
            moveAbsolute (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    position (Variable):
                    maxVelocity (Variable):
                get (Variable):
                    position (Variable):
                    maxVelocity (Variable):
            moveRelative (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    distance (Variable):
                    maxVelocity (Variable):
                get (Variable):
                    distance (Variable):
                    maxVelocity (Variable):
            moveVelocity (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    value (Variable):
                get (Variable):
                    value (Variable):
            reset (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            stop (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            stopParametrized (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    deceleration (Variable):
                    jerk (Variable):
                get (Variable):
                    deceleration (Variable):
                    jerk (Variable):
            power (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    enable (Variable):
                get (Variable):
                    enable (Variable):
            gearIn (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            gearOut (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            initialize (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
            setPosition (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    value (Variable):
                get (Variable):
                    value (Variable):
            enableExtSetGen (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            disableExtSetGen (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            forceCalibration (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            resetCalibration (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            readParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                value (Variable):
                set (Variable):
                    parameterNumber (Variable):
                get (Variable):
                    parameterNumber (Variable):
            readBoolParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                value (Variable):
                set (Variable):
                    parameterNumber (Variable):
                get (Variable):
                    parameterNumber (Variable):
            writeParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    parameterNumber (Variable):
                    value (Variable):
                get (Variable):
                    parameterNumber (Variable):
                    value (Variable):
            writeBoolParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    parameterNumber (Variable):
                    value (Variable):
                get (Variable):
                    parameterNumber (Variable):
                    value (Variable):
            probeStart (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                recordedPosition (Variable):
                set (Variable):
                    risingEdge (Variable):
                    timeout (Variable):
                get (Variable):
                    risingEdge (Variable):
                    timeout (Variable):
            probeAbort (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
        moveAbsolute (Method):
            position (Variable):
            maxVelocity (Variable):
        moveRelative (Method):
            distance (Variable):
            maxVelocity (Variable):
        moveVelocity (Method):
            value (Variable):
        reset (Method):
        stop (Method):
        stopParametrized (Method):
            deceleration (Variable):
            jerk (Variable):
        power (Method):
            enable (Variable):
        gearIn (Method):
        gearOut (Method):
        initialize (Method):
        setPosition (Method):
            value (Variable):
        enableExtSetGen (Method):
        disableExtSetGen (Method):
        forceCalibration (Method):
        resetCalibration (Method):
        readParameter (Method):
            parameterNumber (Variable):
        readBoolParameter (Method):
            parameterNumber (Variable):
        writeParameter (Method):
            parameterNumber (Variable):
            value (Variable):
        writeBoolParameter (Method):
            parameterNumber (Variable):
            value (Variable):
        probeStart (Method):
            risingEdge (Variable):
            timeout (Variable):
        probeAbort (Method):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        actPos (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            millimeters (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            micrometers (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMillimetersValue (Variable):
        actVel (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            millimetersPerSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMillimetersPerSecondValue (Variable):
    LinearAxis (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_LinearAxis
        actualStatus (Variable):
        previousStatus (Variable):
        axis_ref (Variable):
            PlcToNc (Variable):
                ControlDWord (Variable):
                Override (Variable):
                AxisModeRequest (Variable):
                AxisModeDWord (Variable):
                AxisModeLReal (Variable):
                PositionCorrection (Variable):
                ExtSetPos (Variable):
                ExtSetVelo (Variable):
                ExtSetAcc (Variable):
                ExtSetDirection (Variable):
                ExtControllerOutput (Variable):
                GearRatio1 (Variable):
                GearRatio2 (Variable):
                GearRatio3 (Variable):
                GearRatio4 (Variable):
            NcToPlc (Variable):
                StateDWord (Variable):
                ErrorCode (Variable):
                AxisState (Variable):
                AxisModeConfirmation (Variable):
                HomingState (Variable):
                CoupleState (Variable):
                SvbEntries (Variable):
                SafEntries (Variable):
                AxisId (Variable):
                OpModeDWord (Variable):
                ActiveControlLoopIndex (Variable):
                ControlLoopIndex (Variable):
                ActPos (Variable):
                ModuloActPos (Variable):
                ModuloActTurns (Variable):
                ActVelo (Variable):
                PosDiff (Variable):
                SetPos (Variable):
                SetVelo (Variable):
                SetAcc (Variable):
                TargetPos (Variable):
                ModuloSetPos (Variable):
                ModuloSetTurns (Variable):
                CmdNo (Variable):
                CmdState (Variable):
            Status (Variable):
                UpdateTaskIndex (Variable):
                UpdateCycleTime (Variable):
                CycleCounter (Variable):
                NcCycleCounter (Variable):
                Error (Variable):
                ErrorId (Variable):
                Disabled (Variable):
                ControlLoopClosed (Variable):
                ExtSetPointGenEnabled (Variable):
        isGearingSupported (Variable):
        isJogEnabled (Variable):
        isEnabled (Variable):
        standstillTolerance (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            poweredStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            extSetGenStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            motionStatus (Variable):
                superState (Variable):
                actVel (Variable):
                tolerance (Variable):
                forward (Variable):
                backward (Variable):
                standstill (Variable):
        processes (Variable):
            moveAbsolute (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    position (Variable):
                    maxVelocity (Variable):
                get (Variable):
                    position (Variable):
                    maxVelocity (Variable):
            moveRelative (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    distance (Variable):
                    maxVelocity (Variable):
                get (Variable):
                    distance (Variable):
                    maxVelocity (Variable):
            moveVelocity (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    value (Variable):
                get (Variable):
                    value (Variable):
            reset (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            stop (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            stopParametrized (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    deceleration (Variable):
                    jerk (Variable):
                get (Variable):
                    deceleration (Variable):
                    jerk (Variable):
            power (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    enable (Variable):
                get (Variable):
                    enable (Variable):
            gearIn (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            gearOut (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            initialize (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
            setPosition (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    value (Variable):
                get (Variable):
                    value (Variable):
            enableExtSetGen (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            disableExtSetGen (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            forceCalibration (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            resetCalibration (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
            readParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                value (Variable):
                set (Variable):
                    parameterNumber (Variable):
                get (Variable):
                    parameterNumber (Variable):
            readBoolParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                value (Variable):
                set (Variable):
                    parameterNumber (Variable):
                get (Variable):
                    parameterNumber (Variable):
            writeParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    parameterNumber (Variable):
                    value (Variable):
                get (Variable):
                    parameterNumber (Variable):
                    value (Variable):
            writeBoolParameter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    parameterNumber (Variable):
                    value (Variable):
                get (Variable):
                    parameterNumber (Variable):
                    value (Variable):
            probeStart (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                recordedPosition (Variable):
                set (Variable):
                    risingEdge (Variable):
                    timeout (Variable):
                get (Variable):
                    risingEdge (Variable):
                    timeout (Variable):
            probeAbort (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                testVar (Variable):
        moveAbsolute (Method):
            position (Variable):
            maxVelocity (Variable):
        moveRelative (Method):
            distance (Variable):
            maxVelocity (Variable):
        moveVelocity (Method):
            value (Variable):
        reset (Method):
        stop (Method):
        stopParametrized (Method):
            deceleration (Variable):
            jerk (Variable):
        power (Method):
            enable (Variable):
        gearIn (Method):
        gearOut (Method):
        initialize (Method):
        setPosition (Method):
            value (Variable):
        enableExtSetGen (Method):
        disableExtSetGen (Method):
        forceCalibration (Method):
        resetCalibration (Method):
        readParameter (Method):
            parameterNumber (Variable):
        readBoolParameter (Method):
            parameterNumber (Variable):
        writeParameter (Method):
            parameterNumber (Variable):
            value (Variable):
        writeBoolParameter (Method):
            parameterNumber (Variable):
            value (Variable):
        probeStart (Method):
            risingEdge (Variable):
            timeout (Variable):
        probeAbort (Method):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        actPos (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            millimeters (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            micrometers (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMillimetersValue (Variable):
        actVel (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            millimetersPerSecond (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMillimetersPerSecondValue (Variable):
    SDOReadProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        value1Byte (Variable):
        value2Bytes (Variable):
        value4Bytes (Variable):
        errorId (Variable):
        set (Variable):
            index (Variable):
            subindex (Variable):
            noOfBytes (Variable):
        get (Variable):
            index (Variable):
            subindex (Variable):
            noOfBytes (Variable):
        start (Method):
            index (Variable):
            subindex (Variable):
            noOfBytes (Variable):
        request (Method):
            index (Variable):
            subindex (Variable):
            noOfBytes (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    SDOReadProcessArgs (Struct):
        index (Variable):
        subindex (Variable):
        noOfBytes (Variable):
    SDOWriteProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        set (Variable):
            index (Variable):
            subindex (Variable):
            noOfBytes (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
        get (Variable):
            index (Variable):
            subindex (Variable):
            noOfBytes (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
        start (Method):
            index (Variable):
            subindex (Variable):
            noOfBytes (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
        request (Method):
            index (Variable):
            subindex (Variable):
            noOfBytes (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    SDOWriteProcessArgs (Struct):
        index (Variable):
        subindex (Variable):
        noOfBytes (Variable):
        value1Byte (Variable):
        value2Bytes (Variable):
        value4Bytes (Variable):
    FaulhaberDriveConfig (Config):
        sendAtInitialization (Variable):
        CANopenBusNetId (Variable):
        CANopenNodePort (Variable):
        modesOfOperation (Variable):
        polarity (Variable):
        maxProfileVelocity (Variable):
        negativeLimitSwitch (Variable):
        positiveLimitSwitch (Variable):
        homingSwitch (Variable):
        limitSwitchPolarity (Variable):
        continuousCurrentLimit (Variable):
        peakCurrentLimit (Variable):
        velocityControlProportionalTerm (Variable):
        velocityControlIntegralTerm (Variable):
    SM_FaulhaberDrive (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        config (Variable):
            sendAtInitialization (Variable):
            CANopenBusNetId (Variable):
            CANopenNodePort (Variable):
            modesOfOperation (Variable):
            polarity (Variable):
            maxProfileVelocity (Variable):
            negativeLimitSwitch (Variable):
            positiveLimitSwitch (Variable):
            homingSwitch (Variable):
            limitSwitchPolarity (Variable):
            continuousCurrentLimit (Variable):
            peakCurrentLimit (Variable):
            velocityControlProportionalTerm (Variable):
            velocityControlIntegralTerm (Variable):
        actualCurrent (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            amps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliAmps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newAmpsValue (Variable):
        averageCurrent (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            amps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliAmps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newAmpsValue (Variable):
        isEnabled (Variable):
        newMilliampsValue (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        processes (Variable):
            write (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    index (Variable):
                    subindex (Variable):
                    noOfBytes (Variable):
                    value1Byte (Variable):
                    value2Bytes (Variable):
                    value4Bytes (Variable):
                get (Variable):
                    index (Variable):
                    subindex (Variable):
                    noOfBytes (Variable):
                    value1Byte (Variable):
                    value2Bytes (Variable):
                    value4Bytes (Variable):
            read (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
                errorId (Variable):
                set (Variable):
                    index (Variable):
                    subindex (Variable):
                    noOfBytes (Variable):
                get (Variable):
                    index (Variable):
                    subindex (Variable):
                    noOfBytes (Variable):
            initialize (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
        write (Method):
            index (Variable):
            subindex (Variable):
            noOfBytes (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
        read (Method):
            index (Variable):
            subindex (Variable):
            noOfBytes (Variable):
        initialize (Method):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    FaulhaberDriveStatuses (Struct):
        busyStatus (Variable):
            superState (Variable):
            isBusy (Variable):
            idle (Variable):
            busy (Variable):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
    FaulhaberDriveProcesses (Struct):
        write (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            set (Variable):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            get (Variable):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
        read (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
            errorId (Variable):
            set (Variable):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
            get (Variable):
                index (Variable):
                subindex (Variable):
                noOfBytes (Variable):
        initialize (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            testVar (Variable):
    FaulhaberDrive (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_FaulhaberDrive
        actualStatus (Variable):
        previousStatus (Variable):
        config (Variable):
            sendAtInitialization (Variable):
            CANopenBusNetId (Variable):
            CANopenNodePort (Variable):
            modesOfOperation (Variable):
            polarity (Variable):
            maxProfileVelocity (Variable):
            negativeLimitSwitch (Variable):
            positiveLimitSwitch (Variable):
            homingSwitch (Variable):
            limitSwitchPolarity (Variable):
            continuousCurrentLimit (Variable):
            peakCurrentLimit (Variable):
            velocityControlProportionalTerm (Variable):
            velocityControlIntegralTerm (Variable):
        actualCurrent (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            amps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliAmps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newAmpsValue (Variable):
        averageCurrent (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            amps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliAmps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newAmpsValue (Variable):
        isEnabled (Variable):
        newMilliampsValue (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        processes (Variable):
            write (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    index (Variable):
                    subindex (Variable):
                    noOfBytes (Variable):
                    value1Byte (Variable):
                    value2Bytes (Variable):
                    value4Bytes (Variable):
                get (Variable):
                    index (Variable):
                    subindex (Variable):
                    noOfBytes (Variable):
                    value1Byte (Variable):
                    value2Bytes (Variable):
                    value4Bytes (Variable):
            read (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
                errorId (Variable):
                set (Variable):
                    index (Variable):
                    subindex (Variable):
                    noOfBytes (Variable):
                get (Variable):
                    index (Variable):
                    subindex (Variable):
                    noOfBytes (Variable):
            initialize (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
        write (Method):
            index (Variable):
            subindex (Variable):
            noOfBytes (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
        read (Method):
            index (Variable):
            subindex (Variable):
            noOfBytes (Variable):
        initialize (Method):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SoEReadProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        value1Byte (Variable):
        value2Bytes (Variable):
        value4Bytes (Variable):
        adsErrorId (Variable):
        sercosErrorId (Variable):
        set (Variable):
            index (Variable):
            noOfBytes (Variable):
        get (Variable):
            index (Variable):
            noOfBytes (Variable):
        start (Method):
            index (Variable):
            noOfBytes (Variable):
        request (Method):
            index (Variable):
            noOfBytes (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    SoEReadProcessArgs (Struct):
        index (Variable):
        noOfBytes (Variable):
    SoEWriteProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        adsErrorId (Variable):
        sercosErrorId (Variable):
        set (Variable):
            index (Variable):
            noOfBytes (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
        get (Variable):
            index (Variable):
            noOfBytes (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
        start (Method):
            index (Variable):
            noOfBytes (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
        request (Method):
            index (Variable):
            noOfBytes (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    SoEWriteProcessArgs (Struct):
        index (Variable):
        noOfBytes (Variable):
        value1Byte (Variable):
        value2Bytes (Variable):
        value4Bytes (Variable):
    SoEResetProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        adsErrorId (Variable):
        sercosErrorId (Variable):
        start (Method):
        request (Method):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    DriveOperatingModes (Enum):
        OPERATING_MODE_NONE (EnumItem):
        OPERATING_MODE_TORQUE_CONTROL (EnumItem):
        OPERATING_MODE_VELOCITY_CONTROL (EnumItem):
        OPERATING_MODE_POSITION_CONTROL (EnumItem):
    DriveBrakeStates (Enum):
        BRAKE_AUTOMATIC (EnumItem):
        BRAKE_FORCE_LOCK (EnumItem):
        BRAKE_FORCE_UNLOCK (EnumItem):
    SetBrakeProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        set (Variable):
            newState (Variable):
        get (Variable):
            newState (Variable):
        start (Method):
            newState (Variable):
        request (Method):
            newState (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    SetBrakeProcessArgs (Struct):
        newState (Variable):
    SM_AX52XXDriveChannel (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        errorC1D (Variable):
        isEnabled (Variable):
        torqueFeedbackValue (Variable):
        torqueCommandValue (Variable):
        digitalInputs (Variable):
        digitalOutputs (Variable):
        safetyOptionState (Variable):
        diagnosticNumber (Variable):
        isInSafetate (Variable):
        actualTorqueFeedback (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            newtonmeter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newNewtonmeterValue (Variable):
        channelPeakCurrent (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            amps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliAmps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newAmpsValue (Variable):
        channelRatedCurrent (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            amps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliAmps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newAmpsValue (Variable):
        continuousStallTorque (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            newtonmeter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newNewtonmeterValue (Variable):
        continuousStallCurrent (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            amps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliAmps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newAmpsValue (Variable):
        torqueConstant (Variable):
        bipolarTorqueLimit (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            unitless (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            percent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newtonmeter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newFractionValue (Variable):
            maxNewtonmeter (Variable):
        positiveTorqueLimit (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            unitless (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            percent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newtonmeter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newFractionValue (Variable):
            maxNewtonmeter (Variable):
        negativeTorqueLimit (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            unitless (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            percent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newtonmeter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newFractionValue (Variable):
            maxNewtonmeter (Variable):
        operatingMode (Variable):
        operatingModeDescription (Variable):
        brakeState (Variable):
        brakeStateDescription (Variable):
        torqueCommand (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            unitless (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            percent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newtonmeter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newFractionValue (Variable):
            maxNewtonmeter (Variable):
        safetyErrorAck (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        processes (Variable):
            read (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
                adsErrorId (Variable):
                sercosErrorId (Variable):
                set (Variable):
                    index (Variable):
                    noOfBytes (Variable):
                get (Variable):
                    index (Variable):
                    noOfBytes (Variable):
            write (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                adsErrorId (Variable):
                sercosErrorId (Variable):
                set (Variable):
                    index (Variable):
                    noOfBytes (Variable):
                    value1Byte (Variable):
                    value2Bytes (Variable):
                    value4Bytes (Variable):
                get (Variable):
                    index (Variable):
                    noOfBytes (Variable):
                    value1Byte (Variable):
                    value2Bytes (Variable):
                    value4Bytes (Variable):
            update (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
            reset (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                adsErrorId (Variable):
                sercosErrorId (Variable):
            acknowledgeSafetyError (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
            setBrake (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                set (Variable):
                    newState (Variable):
                get (Variable):
                    newState (Variable):
        read (Method):
            index (Variable):
            noOfBytes (Variable):
        write (Method):
            index (Variable):
            noOfBytes (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
        update (Method):
        reset (Method):
        acknowledgeSafetyError (Method):
        setBrake (Method):
            newState (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    AX52XXDriveChannelStatuses (Struct):
        busyStatus (Variable):
            superState (Variable):
            isBusy (Variable):
            idle (Variable):
            busy (Variable):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
    AX52XXDriveChannelProcesses (Struct):
        read (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
            adsErrorId (Variable):
            sercosErrorId (Variable):
            set (Variable):
                index (Variable):
                noOfBytes (Variable):
            get (Variable):
                index (Variable):
                noOfBytes (Variable):
        write (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            adsErrorId (Variable):
            sercosErrorId (Variable):
            set (Variable):
                index (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
            get (Variable):
                index (Variable):
                noOfBytes (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
        update (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            testVar (Variable):
        reset (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            adsErrorId (Variable):
            sercosErrorId (Variable):
        acknowledgeSafetyError (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            testVar (Variable):
        setBrake (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            set (Variable):
                newState (Variable):
            get (Variable):
                newState (Variable):
    AX52XXDriveChannel (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_AX52XXDriveChannel
        actualStatus (Variable):
        previousStatus (Variable):
        errorC1D (Variable):
        isEnabled (Variable):
        torqueFeedbackValue (Variable):
        torqueCommandValue (Variable):
        digitalInputs (Variable):
        digitalOutputs (Variable):
        safetyOptionState (Variable):
        diagnosticNumber (Variable):
        isInSafetate (Variable):
        actualTorqueFeedback (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            newtonmeter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newNewtonmeterValue (Variable):
        channelPeakCurrent (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            amps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliAmps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newAmpsValue (Variable):
        channelRatedCurrent (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            amps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliAmps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newAmpsValue (Variable):
        continuousStallTorque (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            newtonmeter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newNewtonmeterValue (Variable):
        continuousStallCurrent (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            amps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliAmps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newAmpsValue (Variable):
        torqueConstant (Variable):
        bipolarTorqueLimit (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            unitless (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            percent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newtonmeter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newFractionValue (Variable):
            maxNewtonmeter (Variable):
        positiveTorqueLimit (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            unitless (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            percent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newtonmeter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newFractionValue (Variable):
            maxNewtonmeter (Variable):
        negativeTorqueLimit (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            unitless (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            percent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newtonmeter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newFractionValue (Variable):
            maxNewtonmeter (Variable):
        operatingMode (Variable):
        operatingModeDescription (Variable):
        brakeState (Variable):
        brakeStateDescription (Variable):
        torqueCommand (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            unitless (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            percent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newtonmeter (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newFractionValue (Variable):
            maxNewtonmeter (Variable):
        safetyErrorAck (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        processes (Variable):
            read (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                value1Byte (Variable):
                value2Bytes (Variable):
                value4Bytes (Variable):
                adsErrorId (Variable):
                sercosErrorId (Variable):
                set (Variable):
                    index (Variable):
                    noOfBytes (Variable):
                get (Variable):
                    index (Variable):
                    noOfBytes (Variable):
            write (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                adsErrorId (Variable):
                sercosErrorId (Variable):
                set (Variable):
                    index (Variable):
                    noOfBytes (Variable):
                    value1Byte (Variable):
                    value2Bytes (Variable):
                    value4Bytes (Variable):
                get (Variable):
                    index (Variable):
                    noOfBytes (Variable):
                    value1Byte (Variable):
                    value2Bytes (Variable):
                    value4Bytes (Variable):
            update (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
            reset (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                adsErrorId (Variable):
                sercosErrorId (Variable):
            acknowledgeSafetyError (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
            setBrake (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                set (Variable):
                    newState (Variable):
                get (Variable):
                    newState (Variable):
        read (Method):
            index (Variable):
            noOfBytes (Variable):
        write (Method):
            index (Variable):
            noOfBytes (Variable):
            value1Byte (Variable):
            value2Bytes (Variable):
            value4Bytes (Variable):
        update (Method):
        reset (Method):
        acknowledgeSafetyError (Method):
        setBrake (Method):
            newState (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_AX52XXDrive (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        parts (Variable):
            channelA (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                errorC1D (Variable):
                isEnabled (Variable):
                torqueFeedbackValue (Variable):
                torqueCommandValue (Variable):
                digitalInputs (Variable):
                digitalOutputs (Variable):
                safetyOptionState (Variable):
                diagnosticNumber (Variable):
                isInSafetate (Variable):
                actualTorqueFeedback (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newNewtonmeterValue (Variable):
                channelPeakCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                channelRatedCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                continuousStallTorque (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newNewtonmeterValue (Variable):
                continuousStallCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                torqueConstant (Variable):
                bipolarTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                positiveTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                negativeTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                operatingMode (Variable):
                operatingModeDescription (Variable):
                brakeState (Variable):
                brakeStateDescription (Variable):
                torqueCommand (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                safetyErrorAck (Variable):
                statuses (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                processes (Variable):
                    read (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                        set (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                        get (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                    write (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                        set (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                        get (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                    update (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        testVar (Variable):
                    reset (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                    acknowledgeSafetyError (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        testVar (Variable):
                    setBrake (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        set (Variable):
                            newState (Variable):
                        get (Variable):
                            newState (Variable):
            channelB (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                errorC1D (Variable):
                isEnabled (Variable):
                torqueFeedbackValue (Variable):
                torqueCommandValue (Variable):
                digitalInputs (Variable):
                digitalOutputs (Variable):
                safetyOptionState (Variable):
                diagnosticNumber (Variable):
                isInSafetate (Variable):
                actualTorqueFeedback (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newNewtonmeterValue (Variable):
                channelPeakCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                channelRatedCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                continuousStallTorque (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newNewtonmeterValue (Variable):
                continuousStallCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                torqueConstant (Variable):
                bipolarTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                positiveTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                negativeTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                operatingMode (Variable):
                operatingModeDescription (Variable):
                brakeState (Variable):
                brakeStateDescription (Variable):
                torqueCommand (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                safetyErrorAck (Variable):
                statuses (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                processes (Variable):
                    read (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                        set (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                        get (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                    write (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                        set (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                        get (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                    update (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        testVar (Variable):
                    reset (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                    acknowledgeSafetyError (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        testVar (Variable):
                    setBrake (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        set (Variable):
                            newState (Variable):
                        get (Variable):
                            newState (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    AX52XXDriveStatuses (Struct):
        busyStatus (Variable):
            superState (Variable):
            isBusy (Variable):
            idle (Variable):
            busy (Variable):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
    AX52XXDriveParts (Struct):
        channelA (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            errorC1D (Variable):
            isEnabled (Variable):
            torqueFeedbackValue (Variable):
            torqueCommandValue (Variable):
            digitalInputs (Variable):
            digitalOutputs (Variable):
            safetyOptionState (Variable):
            diagnosticNumber (Variable):
            isInSafetate (Variable):
            actualTorqueFeedback (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newNewtonmeterValue (Variable):
            channelPeakCurrent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            channelRatedCurrent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            continuousStallTorque (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newNewtonmeterValue (Variable):
            continuousStallCurrent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            torqueConstant (Variable):
            bipolarTorqueLimit (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
                maxNewtonmeter (Variable):
            positiveTorqueLimit (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
                maxNewtonmeter (Variable):
            negativeTorqueLimit (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
                maxNewtonmeter (Variable):
            operatingMode (Variable):
            operatingModeDescription (Variable):
            brakeState (Variable):
            brakeStateDescription (Variable):
            torqueCommand (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
                maxNewtonmeter (Variable):
            safetyErrorAck (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            processes (Variable):
                read (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value1Byte (Variable):
                    value2Bytes (Variable):
                    value4Bytes (Variable):
                    adsErrorId (Variable):
                    sercosErrorId (Variable):
                    set (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                    get (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                write (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    adsErrorId (Variable):
                    sercosErrorId (Variable):
                    set (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                    get (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                update (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                reset (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    adsErrorId (Variable):
                    sercosErrorId (Variable):
                acknowledgeSafetyError (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                setBrake (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    set (Variable):
                        newState (Variable):
                    get (Variable):
                        newState (Variable):
        channelB (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            errorC1D (Variable):
            isEnabled (Variable):
            torqueFeedbackValue (Variable):
            torqueCommandValue (Variable):
            digitalInputs (Variable):
            digitalOutputs (Variable):
            safetyOptionState (Variable):
            diagnosticNumber (Variable):
            isInSafetate (Variable):
            actualTorqueFeedback (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newNewtonmeterValue (Variable):
            channelPeakCurrent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            channelRatedCurrent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            continuousStallTorque (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newNewtonmeterValue (Variable):
            continuousStallCurrent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                amps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                milliAmps (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newAmpsValue (Variable):
            torqueConstant (Variable):
            bipolarTorqueLimit (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
                maxNewtonmeter (Variable):
            positiveTorqueLimit (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
                maxNewtonmeter (Variable):
            negativeTorqueLimit (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
                maxNewtonmeter (Variable):
            operatingMode (Variable):
            operatingModeDescription (Variable):
            brakeState (Variable):
            brakeStateDescription (Variable):
            torqueCommand (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                unitless (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                percent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newtonmeter (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    value (Variable):
                    unit (Variable):
                newFractionValue (Variable):
                maxNewtonmeter (Variable):
            safetyErrorAck (Variable):
            statuses (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
            processes (Variable):
                read (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    value1Byte (Variable):
                    value2Bytes (Variable):
                    value4Bytes (Variable):
                    adsErrorId (Variable):
                    sercosErrorId (Variable):
                    set (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                    get (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                write (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    adsErrorId (Variable):
                    sercosErrorId (Variable):
                    set (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                    get (Variable):
                        index (Variable):
                        noOfBytes (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                update (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                reset (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    adsErrorId (Variable):
                    sercosErrorId (Variable):
                acknowledgeSafetyError (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    testVar (Variable):
                setBrake (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    isEnabled (Variable):
                    statuses (Variable):
                        enabledStatus (Variable):
                            superState (Variable):
                            isEnabled (Variable):
                            enabled (Variable):
                            disabled (Variable):
                        busyStatus (Variable):
                            superState (Variable):
                            isBusy (Variable):
                            idle (Variable):
                            busy (Variable):
                        healthStatus (Variable):
                            superState (Variable):
                            isGood (Variable):
                            hasWarning (Variable):
                            good (Variable):
                            warning (Variable):
                            bad (Variable):
                        startedStatus (Variable):
                            superState (Variable):
                            isStarted (Variable):
                            started (Variable):
                            notStarted (Variable):
                    do_request (Variable):
                    do_request_result (Variable):
                    set (Variable):
                        newState (Variable):
                    get (Variable):
                        newState (Variable):
    AX52XXDrive (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_AX52XXDrive
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        parts (Variable):
            channelA (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                errorC1D (Variable):
                isEnabled (Variable):
                torqueFeedbackValue (Variable):
                torqueCommandValue (Variable):
                digitalInputs (Variable):
                digitalOutputs (Variable):
                safetyOptionState (Variable):
                diagnosticNumber (Variable):
                isInSafetate (Variable):
                actualTorqueFeedback (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newNewtonmeterValue (Variable):
                channelPeakCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                channelRatedCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                continuousStallTorque (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newNewtonmeterValue (Variable):
                continuousStallCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                torqueConstant (Variable):
                bipolarTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                positiveTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                negativeTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                operatingMode (Variable):
                operatingModeDescription (Variable):
                brakeState (Variable):
                brakeStateDescription (Variable):
                torqueCommand (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                safetyErrorAck (Variable):
                statuses (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                processes (Variable):
                    read (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                        set (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                        get (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                    write (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                        set (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                        get (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                    update (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        testVar (Variable):
                    reset (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                    acknowledgeSafetyError (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        testVar (Variable):
                    setBrake (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        set (Variable):
                            newState (Variable):
                        get (Variable):
                            newState (Variable):
            channelB (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                errorC1D (Variable):
                isEnabled (Variable):
                torqueFeedbackValue (Variable):
                torqueCommandValue (Variable):
                digitalInputs (Variable):
                digitalOutputs (Variable):
                safetyOptionState (Variable):
                diagnosticNumber (Variable):
                isInSafetate (Variable):
                actualTorqueFeedback (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newNewtonmeterValue (Variable):
                channelPeakCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                channelRatedCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                continuousStallTorque (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newNewtonmeterValue (Variable):
                continuousStallCurrent (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    amps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    milliAmps (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newAmpsValue (Variable):
                torqueConstant (Variable):
                bipolarTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                positiveTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                negativeTorqueLimit (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                operatingMode (Variable):
                operatingModeDescription (Variable):
                brakeState (Variable):
                brakeStateDescription (Variable):
                torqueCommand (Variable):
                    actualStatus (Variable):
                    previousStatus (Variable):
                    unitless (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    percent (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newtonmeter (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        value (Variable):
                        unit (Variable):
                    newFractionValue (Variable):
                    maxNewtonmeter (Variable):
                safetyErrorAck (Variable):
                statuses (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                processes (Variable):
                    read (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        value1Byte (Variable):
                        value2Bytes (Variable):
                        value4Bytes (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                        set (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                        get (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                    write (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                        set (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                        get (Variable):
                            index (Variable):
                            noOfBytes (Variable):
                            value1Byte (Variable):
                            value2Bytes (Variable):
                            value4Bytes (Variable):
                    update (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        testVar (Variable):
                    reset (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        adsErrorId (Variable):
                        sercosErrorId (Variable):
                    acknowledgeSafetyError (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        testVar (Variable):
                    setBrake (Variable):
                        actualStatus (Variable):
                        previousStatus (Variable):
                        isEnabled (Variable):
                        statuses (Variable):
                            enabledStatus (Variable):
                                superState (Variable):
                                isEnabled (Variable):
                                enabled (Variable):
                                disabled (Variable):
                            busyStatus (Variable):
                                superState (Variable):
                                isBusy (Variable):
                                idle (Variable):
                                busy (Variable):
                            healthStatus (Variable):
                                superState (Variable):
                                isGood (Variable):
                                hasWarning (Variable):
                                good (Variable):
                                warning (Variable):
                                bad (Variable):
                            startedStatus (Variable):
                                superState (Variable):
                                isStarted (Variable):
                                started (Variable):
                                notStarted (Variable):
                        do_request (Variable):
                        do_request_result (Variable):
                        set (Variable):
                            newState (Variable):
                        get (Variable):
                            newState (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_SSIEncoder (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        dataError (Variable):
        frameError (Variable):
        powerFailure (Variable):
        syncError (Variable):
        counterValue (Variable):
        status (Variable):
        statuses (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SSIEncoderStatuses (Struct):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
    SSIEncoder (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_SSIEncoder
        actualStatus (Variable):
        previousStatus (Variable):
        dataError (Variable):
        frameError (Variable):
        powerFailure (Variable):
        syncError (Variable):
        counterValue (Variable):
        status (Variable):
        statuses (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_IncrementalEncoder (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        latchCValid (Variable):
        latchExternValid (Variable):
        setCounterDone (Variable):
        counterUnderflow (Variable):
        counterOverflow (Variable):
        statusOfInputStatus (Variable):
        openCircuit (Variable):
        extrapolationStall (Variable):
        statusOfInputA (Variable):
        statusOfInputB (Variable):
        statusOfInputC (Variable):
        statusOfInputGate (Variable):
        statusOfExternLatch (Variable):
        syncError (Variable):
        isEnabled (Variable):
        counterValue (Variable):
        status (Variable):
        setCounterValue (Variable):
        setCounter (Variable):
        enableLatchC (Variable):
        statuses (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
        processes (Variable):
            enableCounterResetC (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
            disableCounterResetC (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
        enableCounterResetC (Method):
        disableCounterResetC (Method):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    IncrementalEncoderStatuses (Struct):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
        busyStatus (Variable):
            superState (Variable):
            isBusy (Variable):
            idle (Variable):
            busy (Variable):
    IncrementalEncoderProcesses (Struct):
        enableCounterResetC (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            testVar (Variable):
        disableCounterResetC (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            testVar (Variable):
    IncrementalEncoder (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_IncrementalEncoder
        actualStatus (Variable):
        previousStatus (Variable):
        latchCValid (Variable):
        latchExternValid (Variable):
        setCounterDone (Variable):
        counterUnderflow (Variable):
        counterOverflow (Variable):
        statusOfInputStatus (Variable):
        openCircuit (Variable):
        extrapolationStall (Variable):
        statusOfInputA (Variable):
        statusOfInputB (Variable):
        statusOfInputC (Variable):
        statusOfInputGate (Variable):
        statusOfExternLatch (Variable):
        syncError (Variable):
        isEnabled (Variable):
        counterValue (Variable):
        status (Variable):
        setCounterValue (Variable):
        setCounter (Variable):
        enableLatchC (Variable):
        statuses (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
        processes (Variable):
            enableCounterResetC (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
            disableCounterResetC (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
        enableCounterResetC (Method):
        disableCounterResetC (Method):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_EtherCatDevice (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        id (Variable):
        typeId (Variable):
        wcState (Variable):
        infoData (Variable):
        statuses (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    EtherCatDeviceStatuses (Struct):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
    EtherCatDevice (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_EtherCatDevice
        actualStatus (Variable):
        previousStatus (Variable):
        id (Variable):
        typeId (Variable):
        wcState (Variable):
        infoData (Variable):
        statuses (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_CANopenBus (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        error (Variable):
        canState (Variable):
        rxErrorCounter (Variable):
        txErrorCounter (Variable):
        statuses (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    CANopenBusStatuses (Struct):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
    CANopenBus (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_CANopenBus
        actualStatus (Variable):
        previousStatus (Variable):
        error (Variable):
        canState (Variable):
        rxErrorCounter (Variable):
        txErrorCounter (Variable):
        statuses (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_CurrentMeasurement (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        current (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            amps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliAmps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newAmpsValue (Variable):
        microAmpsValue (Variable):
        error (Variable):
        statuses (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    CurrentMeasurementStatuses (Struct):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
    CurrentMeasurement (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_CurrentMeasurement
        actualStatus (Variable):
        previousStatus (Variable):
        current (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            amps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliAmps (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newAmpsValue (Variable):
        microAmpsValue (Variable):
        error (Variable):
        statuses (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_VoltageMeasurement (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            volt (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliVolt (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newVoltValue (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            volt (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliVolt (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newVoltValue (Variable):
        underrange (Variable):
        overrange (Variable):
        rawValue (Variable):
        error (Variable):
        conversionFactor (Variable):
        config (Variable):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    VoltageMeasurementStatuses (Struct):
        enabledStatus (Variable):
            superState (Variable):
            isEnabled (Variable):
            enabled (Variable):
            disabled (Variable):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
        alarmStatus (Variable):
            superState (Variable):
            config (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
            value (Variable):
            disabled (Variable):
            hiHi (Variable):
            hi (Variable):
            ok (Variable):
            lo (Variable):
            loLo (Variable):
    VoltageMeasurement (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_VoltageMeasurement
        actualStatus (Variable):
        previousStatus (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            volt (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliVolt (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newVoltValue (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            volt (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliVolt (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newVoltValue (Variable):
        underrange (Variable):
        overrange (Variable):
        rawValue (Variable):
        error (Variable):
        conversionFactor (Variable):
        config (Variable):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_TemperatureMeasurement (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            celsius (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            kelvin (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newCelsiusValue (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            celsius (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            kelvin (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newCelsiusValue (Variable):
        underrange (Variable):
        overrange (Variable):
        rawValue (Variable):
        error (Variable):
        conversionFactor (Variable):
        config (Variable):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    TemperatureMeasurementStatuses (Struct):
        enabledStatus (Variable):
            superState (Variable):
            isEnabled (Variable):
            enabled (Variable):
            disabled (Variable):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
        alarmStatus (Variable):
            superState (Variable):
            config (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
            value (Variable):
            disabled (Variable):
            hiHi (Variable):
            hi (Variable):
            ok (Variable):
            lo (Variable):
            loLo (Variable):
    TemperatureMeasurement (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_TemperatureMeasurement
        actualStatus (Variable):
        previousStatus (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            celsius (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            kelvin (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newCelsiusValue (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            celsius (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            kelvin (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newCelsiusValue (Variable):
        underrange (Variable):
        overrange (Variable):
        rawValue (Variable):
        error (Variable):
        conversionFactor (Variable):
        config (Variable):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_PressureMeasurement (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            bar (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            pascal (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            hectoPascal (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newBarValue (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            bar (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            pascal (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            hectoPascal (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newBarValue (Variable):
        underrange (Variable):
        overrange (Variable):
        rawValue (Variable):
        error (Variable):
        conversionFactor (Variable):
        config (Variable):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    PressureMeasurementStatuses (Struct):
        enabledStatus (Variable):
            superState (Variable):
            isEnabled (Variable):
            enabled (Variable):
            disabled (Variable):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
        alarmStatus (Variable):
            superState (Variable):
            config (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
            value (Variable):
            disabled (Variable):
            hiHi (Variable):
            hi (Variable):
            ok (Variable):
            lo (Variable):
            loLo (Variable):
    PressureMeasurement (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_PressureMeasurement
        actualStatus (Variable):
        previousStatus (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            bar (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            pascal (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            hectoPascal (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newBarValue (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            bar (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            pascal (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            hectoPascal (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newBarValue (Variable):
        underrange (Variable):
        overrange (Variable):
        rawValue (Variable):
        error (Variable):
        conversionFactor (Variable):
        config (Variable):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_FrequencyMeasurement (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            hertz (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newHertzValue (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            hertz (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newHertzValue (Variable):
        underrange (Variable):
        overrange (Variable):
        rawValue (Variable):
        error (Variable):
        conversionFactor (Variable):
        config (Variable):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    FrequencyMeasurementStatuses (Struct):
        enabledStatus (Variable):
            superState (Variable):
            isEnabled (Variable):
            enabled (Variable):
            disabled (Variable):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
        alarmStatus (Variable):
            superState (Variable):
            config (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
            value (Variable):
            disabled (Variable):
            hiHi (Variable):
            hi (Variable):
            ok (Variable):
            lo (Variable):
            loLo (Variable):
    FrequencyMeasurement (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_FrequencyMeasurement
        actualStatus (Variable):
        previousStatus (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            hertz (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newHertzValue (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            hertz (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newHertzValue (Variable):
        underrange (Variable):
        overrange (Variable):
        rawValue (Variable):
        error (Variable):
        conversionFactor (Variable):
        config (Variable):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_GForceMeasurement (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            g (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliG (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMilliGValue (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            g (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliG (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMilliGValue (Variable):
        milliGValue (Variable):
        error (Variable):
        statuses (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    GForceMeasurementStatuses (Struct):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
    GForceMeasurement (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_GForceMeasurement
        actualStatus (Variable):
        previousStatus (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            g (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliG (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMilliGValue (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            g (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            milliG (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMilliGValue (Variable):
        milliGValue (Variable):
        error (Variable):
        statuses (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_RelativeHumidityMeasurement (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            unitless (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            percent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newFractionValue (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            unitless (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            percent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newFractionValue (Variable):
        underrange (Variable):
        overrange (Variable):
        rawValue (Variable):
        error (Variable):
        conversionFactor (Variable):
        config (Variable):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    RelativeHumidityMeasurementStatuses (Struct):
        enabledStatus (Variable):
            superState (Variable):
            isEnabled (Variable):
            enabled (Variable):
            disabled (Variable):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
        alarmStatus (Variable):
            superState (Variable):
            config (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
            value (Variable):
            disabled (Variable):
            hiHi (Variable):
            hi (Variable):
            ok (Variable):
            lo (Variable):
            loLo (Variable):
    RelativeHumidityMeasurement (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_RelativeHumidityMeasurement
        actualStatus (Variable):
        previousStatus (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            unitless (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            percent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newFractionValue (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            unitless (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            percent (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newFractionValue (Variable):
        underrange (Variable):
        overrange (Variable):
        rawValue (Variable):
        error (Variable):
        conversionFactor (Variable):
        config (Variable):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_ForceMeasurement (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            newton (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            decaNewton (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newNewtonValue (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            newton (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            decaNewton (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newNewtonValue (Variable):
        rawValue (Variable):
        error (Variable):
        underrange (Variable):
        overrange (Variable):
        conversionFactor (Variable):
        config (Variable):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    ForceMeasurementStatuses (Struct):
        enabledStatus (Variable):
            superState (Variable):
            isEnabled (Variable):
            enabled (Variable):
            disabled (Variable):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
        alarmStatus (Variable):
            superState (Variable):
            config (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
            value (Variable):
            disabled (Variable):
            hiHi (Variable):
            hi (Variable):
            ok (Variable):
            lo (Variable):
            loLo (Variable):
    ForceMeasurement (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_ForceMeasurement
        actualStatus (Variable):
        previousStatus (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            newton (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            decaNewton (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newNewtonValue (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            newton (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            decaNewton (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newNewtonValue (Variable):
        rawValue (Variable):
        error (Variable):
        underrange (Variable):
        overrange (Variable):
        conversionFactor (Variable):
        config (Variable):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_LinearPositionMeasurement16 (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            millimeters (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            micrometers (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMillimetersValue (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            millimeters (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            micrometers (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMillimetersValue (Variable):
        rawValue (Variable):
        error (Variable):
        underrange (Variable):
        overrange (Variable):
        conversionFactor (Variable):
        config (Variable):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    LinearPositionMeasurement16Statuses (Struct):
        enabledStatus (Variable):
            superState (Variable):
            isEnabled (Variable):
            enabled (Variable):
            disabled (Variable):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
        alarmStatus (Variable):
            superState (Variable):
            config (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
            value (Variable):
            disabled (Variable):
            hiHi (Variable):
            hi (Variable):
            ok (Variable):
            lo (Variable):
            loLo (Variable):
    LinearPositionMeasurement16 (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_LinearPositionMeasurement16
        actualStatus (Variable):
        previousStatus (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            millimeters (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            micrometers (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMillimetersValue (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            millimeters (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            micrometers (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMillimetersValue (Variable):
        rawValue (Variable):
        error (Variable):
        underrange (Variable):
        overrange (Variable):
        conversionFactor (Variable):
        config (Variable):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_LinearPositionMeasurementU32 (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            millimeters (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            micrometers (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMillimetersValue (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            millimeters (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            micrometers (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMillimetersValue (Variable):
        rawValue (Variable):
        error (Variable):
        underrange (Variable):
        overrange (Variable):
        conversionFactor (Variable):
        config (Variable):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    LinearPositionMeasurementU32Statuses (Struct):
        enabledStatus (Variable):
            superState (Variable):
            isEnabled (Variable):
            enabled (Variable):
            disabled (Variable):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
        alarmStatus (Variable):
            superState (Variable):
            config (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
            value (Variable):
            disabled (Variable):
            hiHi (Variable):
            hi (Variable):
            ok (Variable):
            lo (Variable):
            loLo (Variable):
    LinearPositionMeasurementU32 (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_LinearPositionMeasurementU32
        actualStatus (Variable):
        previousStatus (Variable):
        actual (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            millimeters (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            micrometers (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMillimetersValue (Variable):
        average (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            millimeters (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            micrometers (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                value (Variable):
                unit (Variable):
            newMillimetersValue (Variable):
        rawValue (Variable):
        error (Variable):
        underrange (Variable):
        overrange (Variable):
        conversionFactor (Variable):
        config (Variable):
            enabled (Variable):
            gain (Variable):
            offset (Variable):
            alarms (Variable):
                enabled (Variable):
                loLo (Variable):
                lo (Variable):
                hi (Variable):
                hiHi (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            alarmStatus (Variable):
                superState (Variable):
                config (Variable):
                    enabled (Variable):
                    loLo (Variable):
                    lo (Variable):
                    hi (Variable):
                    hiHi (Variable):
                value (Variable):
                disabled (Variable):
                hiHi (Variable):
                hi (Variable):
                ok (Variable):
                lo (Variable):
                loLo (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SM_SimpleRelay (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        digitalOutput (Variable):
        isEnabled (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
        processes (Variable):
            setEnabled (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                set (Variable):
                    enabled (Variable):
                get (Variable):
                    enabled (Variable):
        setEnabled (Method):
            enabled (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    SimpleRelayStatuses (Struct):
        busyStatus (Variable):
            superState (Variable):
            isBusy (Variable):
            idle (Variable):
            busy (Variable):
    SimpleRelayProcesses (Struct):
        setEnabled (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            set (Variable):
                enabled (Variable):
            get (Variable):
                enabled (Variable):
    SimpleRelay (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_SimpleRelay
        actualStatus (Variable):
        previousStatus (Variable):
        digitalOutput (Variable):
        isEnabled (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
        processes (Variable):
            setEnabled (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                set (Variable):
                    enabled (Variable):
                get (Variable):
                    enabled (Variable):
        setEnabled (Method):
            enabled (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    ChangeOperatingStateProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        set (Variable):
            state (Variable):
        get (Variable):
            state (Variable):
        start (Method):
            state (Variable):
        request (Method):
            state (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    ChangeOperatingStateProcessArgs (Struct):
        state (Variable):
    ChangeOperatorStateProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        set (Variable):
            state (Variable):
            password (Variable):
        get (Variable):
            state (Variable):
            password (Variable):
        start (Method):
            state (Variable):
            password (Variable):
        request (Method):
            state (Variable):
            password (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    ChangeOperatorStateProcessArgs (Struct):
        state (Variable):
        password (Variable):
    ChangeOperatingModeStateProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        set (Variable):
            state (Variable):
        get (Variable):
            state (Variable):
        start (Method):
            state (Variable):
        request (Method):
            state (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    ChangeOperatingModeStateProcessArgs (Struct):
        state (Variable):
    ReadXmlProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseXmlDataSrvProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        set (Variable):
            filePath (Variable):
        get (Variable):
            filePath (Variable):
        start (Method):
            filePath (Variable):
        request (Method):
            filePath (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    ReadXmlProcessArgs (Struct):
        filePath (Variable):
    WriteXmlProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseXmlDataSrvProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        set (Variable):
            filePath (Variable):
        get (Variable):
            filePath (Variable):
        start (Method):
            filePath (Variable):
        request (Method):
            filePath (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    WriteXmlProcessArgs (Struct):
        filePath (Variable):
    ActivateProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        testVar (Variable):
        start (Method):
        request (Method):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    SM_ConfigManager (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        filePath (Variable):
        isEnabled (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        processes (Variable):
            save (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    filePath (Variable):
                get (Variable):
                    filePath (Variable):
            load (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    filePath (Variable):
                get (Variable):
                    filePath (Variable):
            activate (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
        save (Method):
            filePath (Variable):
        load (Method):
            filePath (Variable):
        activate (Method):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    ConfigManagerStatuses (Struct):
        busyStatus (Variable):
            superState (Variable):
            isBusy (Variable):
            idle (Variable):
            busy (Variable):
        healthStatus (Variable):
            superState (Variable):
            isGood (Variable):
            hasWarning (Variable):
            good (Variable):
            warning (Variable):
            bad (Variable):
    ConfigManagerProcesses (Struct):
        save (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            set (Variable):
                filePath (Variable):
            get (Variable):
                filePath (Variable):
        load (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            set (Variable):
                filePath (Variable):
            get (Variable):
                filePath (Variable):
        activate (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            testVar (Variable):
    ConfigManager (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_ConfigManager
        actualStatus (Variable):
        previousStatus (Variable):
        filePath (Variable):
        isEnabled (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
        processes (Variable):
            save (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    filePath (Variable):
                get (Variable):
                    filePath (Variable):
            load (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    filePath (Variable):
                get (Variable):
                    filePath (Variable):
            activate (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                testVar (Variable):
        save (Method):
            filePath (Variable):
        load (Method):
            filePath (Variable):
        activate (Method):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    InstrumentConfig (Config):
        name (Variable):
        doInitialThermalFocus (Variable):
        changeM3 (Variable):
        moveKnownPosition (Variable):
        moveKnownPositionName (Variable):
        powerOnRoc (Variable):
        powerOffRoc (Variable):
        powerOnRon (Variable):
        powerOffRon (Variable):
        loadPointingModel (Variable):
        rocActive (Variable):
        ronActive (Variable):
    ModbusRTUBusReadCoilProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        value (Variable):
        errorId (Variable):
        set (Variable):
            unitID (Variable):
            address (Variable):
        get (Variable):
            unitID (Variable):
            address (Variable):
        start (Method):
            unitID (Variable):
            address (Variable):
        request (Method):
            unitID (Variable):
            address (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    ModbusRTUBusReadCoilProcessArgs (Struct):
        unitID (Variable):
        address (Variable):
    ModbusRTUBusWriteCoilProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        set (Variable):
            unitID (Variable):
            address (Variable):
            value (Variable):
        get (Variable):
            unitID (Variable):
            address (Variable):
            value (Variable):
        start (Method):
            unitID (Variable):
            address (Variable):
            value (Variable):
        request (Method):
            unitID (Variable):
            address (Variable):
            value (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    ModbusRTUBusWriteCoilProcessArgs (Struct):
        unitID (Variable):
        address (Variable):
        value (Variable):
    ModbusRTUBusReadRegisterProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        value (Variable):
        errorId (Variable):
        set (Variable):
            unitID (Variable):
            address (Variable):
        get (Variable):
            unitID (Variable):
            address (Variable):
        start (Method):
            unitID (Variable):
            address (Variable):
        request (Method):
            unitID (Variable):
            address (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    ModbusRTUBusReadRegisterProcessArgs (Struct):
        unitID (Variable):
        address (Variable):
    ModbusRTUBusWriteRegisterProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        errorId (Variable):
        set (Variable):
            unitID (Variable):
            address (Variable):
            value (Variable):
        get (Variable):
            unitID (Variable):
            address (Variable):
            value (Variable):
        start (Method):
            unitID (Variable):
            address (Variable):
            value (Variable):
        request (Method):
            unitID (Variable):
            address (Variable):
            value (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    ModbusRTUBusWriteRegisterProcessArgs (Struct):
        unitID (Variable):
        address (Variable):
        value (Variable):
    ModbusRTUBusDiagnosticsProcess (Process):
        SUPER (Pointer):
           points_to_type: BaseProcess
        actualStatus (Variable):
        previousStatus (Variable):
        isEnabled (Variable):
        statuses (Variable):
            enabledStatus (Variable):
                superState (Variable):
                isEnabled (Variable):
                enabled (Variable):
                disabled (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
            healthStatus (Variable):
                superState (Variable):
                isGood (Variable):
                hasWarning (Variable):
                good (Variable):
                warning (Variable):
                bad (Variable):
            startedStatus (Variable):
                superState (Variable):
                isStarted (Variable):
                started (Variable):
                notStarted (Variable):
        do_request (Variable):
        do_request_result (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
        value (Variable):
        errorId (Variable):
        set (Variable):
            unitID (Variable):
            subFunction (Variable):
            subData (Variable):
        get (Variable):
            unitID (Variable):
            subFunction (Variable):
            subData (Variable):
        start (Method):
            unitID (Variable):
            subFunction (Variable):
            subData (Variable):
        request (Method):
            unitID (Variable):
            subFunction (Variable):
            subData (Variable):
            ifthen (IfThen):
                enabled (Variable):
        ifthen (IfThen):
            do_request (Variable):
    ModbusRTUBusDiagnosticsProcessArgs (Struct):
        unitID (Variable):
        subFunction (Variable):
        subData (Variable):
    SM_ModbusRTUBus (Statemachine):
        actualStatus (Variable):
        previousStatus (Variable):
        retries (Variable):
        isEnabled (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
        processes (Variable):
            readCoil (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                value (Variable):
                errorId (Variable):
                set (Variable):
                    unitID (Variable):
                    address (Variable):
                get (Variable):
                    unitID (Variable):
                    address (Variable):
            writeCoil (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    unitID (Variable):
                    address (Variable):
                    value (Variable):
                get (Variable):
                    unitID (Variable):
                    address (Variable):
                    value (Variable):
            readRegister (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                value (Variable):
                errorId (Variable):
                set (Variable):
                    unitID (Variable):
                    address (Variable):
                get (Variable):
                    unitID (Variable):
                    address (Variable):
            writeRegister (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    unitID (Variable):
                    address (Variable):
                    value (Variable):
                get (Variable):
                    unitID (Variable):
                    address (Variable):
                    value (Variable):
            diagnostics (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                value (Variable):
                errorId (Variable):
                set (Variable):
                    unitID (Variable):
                    subFunction (Variable):
                    subData (Variable):
                get (Variable):
                    unitID (Variable):
                    subFunction (Variable):
                    subData (Variable):
        readCoil (Method):
            unitID (Variable):
            address (Variable):
        writeCoil (Method):
            unitID (Variable):
            address (Variable):
            value (Variable):
        readRegister (Method):
            unitID (Variable):
            address (Variable):
        writeRegister (Method):
            unitID (Variable):
            address (Variable):
            value (Variable):
        diagnostics (Method):
            unitID (Variable):
            subFunction (Variable):
            subData (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):
    ModbusRTUBusStatuses (Struct):
        busyStatus (Variable):
            superState (Variable):
            isBusy (Variable):
            idle (Variable):
            busy (Variable):
    ModbusRTUBusProcesses (Struct):
        readCoil (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            value (Variable):
            errorId (Variable):
            set (Variable):
                unitID (Variable):
                address (Variable):
            get (Variable):
                unitID (Variable):
                address (Variable):
        writeCoil (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            set (Variable):
                unitID (Variable):
                address (Variable):
                value (Variable):
            get (Variable):
                unitID (Variable):
                address (Variable):
                value (Variable):
        readRegister (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            value (Variable):
            errorId (Variable):
            set (Variable):
                unitID (Variable):
                address (Variable):
            get (Variable):
                unitID (Variable):
                address (Variable):
        writeRegister (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            errorId (Variable):
            set (Variable):
                unitID (Variable):
                address (Variable):
                value (Variable):
            get (Variable):
                unitID (Variable):
                address (Variable):
                value (Variable):
        diagnostics (Variable):
            actualStatus (Variable):
            previousStatus (Variable):
            isEnabled (Variable):
            statuses (Variable):
                enabledStatus (Variable):
                    superState (Variable):
                    isEnabled (Variable):
                    enabled (Variable):
                    disabled (Variable):
                busyStatus (Variable):
                    superState (Variable):
                    isBusy (Variable):
                    idle (Variable):
                    busy (Variable):
                healthStatus (Variable):
                    superState (Variable):
                    isGood (Variable):
                    hasWarning (Variable):
                    good (Variable):
                    warning (Variable):
                    bad (Variable):
                startedStatus (Variable):
                    superState (Variable):
                    isStarted (Variable):
                    started (Variable):
                    notStarted (Variable):
            do_request (Variable):
            do_request_result (Variable):
            value (Variable):
            errorId (Variable):
            set (Variable):
                unitID (Variable):
                subFunction (Variable):
                subData (Variable):
            get (Variable):
                unitID (Variable):
                subFunction (Variable):
                subData (Variable):
    ModbusRTUBus (FunctionBlock):
        SUPER (Pointer):
           points_to_type: SM_ModbusRTUBus
        actualStatus (Variable):
        previousStatus (Variable):
        retries (Variable):
        isEnabled (Variable):
        statuses (Variable):
            busyStatus (Variable):
                superState (Variable):
                isBusy (Variable):
                idle (Variable):
                busy (Variable):
        processes (Variable):
            readCoil (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                value (Variable):
                errorId (Variable):
                set (Variable):
                    unitID (Variable):
                    address (Variable):
                get (Variable):
                    unitID (Variable):
                    address (Variable):
            writeCoil (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    unitID (Variable):
                    address (Variable):
                    value (Variable):
                get (Variable):
                    unitID (Variable):
                    address (Variable):
                    value (Variable):
            readRegister (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                value (Variable):
                errorId (Variable):
                set (Variable):
                    unitID (Variable):
                    address (Variable):
                get (Variable):
                    unitID (Variable):
                    address (Variable):
            writeRegister (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                errorId (Variable):
                set (Variable):
                    unitID (Variable):
                    address (Variable):
                    value (Variable):
                get (Variable):
                    unitID (Variable):
                    address (Variable):
                    value (Variable):
            diagnostics (Variable):
                actualStatus (Variable):
                previousStatus (Variable):
                isEnabled (Variable):
                statuses (Variable):
                    enabledStatus (Variable):
                        superState (Variable):
                        isEnabled (Variable):
                        enabled (Variable):
                        disabled (Variable):
                    busyStatus (Variable):
                        superState (Variable):
                        isBusy (Variable):
                        idle (Variable):
                        busy (Variable):
                    healthStatus (Variable):
                        superState (Variable):
                        isGood (Variable):
                        hasWarning (Variable):
                        good (Variable):
                        warning (Variable):
                        bad (Variable):
                    startedStatus (Variable):
                        superState (Variable):
                        isStarted (Variable):
                        started (Variable):
                        notStarted (Variable):
                do_request (Variable):
                do_request_result (Variable):
                value (Variable):
                errorId (Variable):
                set (Variable):
                    unitID (Variable):
                    subFunction (Variable):
                    subData (Variable):
                get (Variable):
                    unitID (Variable):
                    subFunction (Variable):
                    subData (Variable):
        readCoil (Method):
            unitID (Variable):
            address (Variable):
        writeCoil (Method):
            unitID (Variable):
            address (Variable):
            value (Variable):
        readRegister (Method):
            unitID (Variable):
            address (Variable):
        writeRegister (Method):
            unitID (Variable):
            address (Variable):
            value (Variable):
        diagnostics (Method):
            unitID (Variable):
            subFunction (Variable):
            subData (Variable):
        _log (Method):
            name (Variable):
            buffer (Variable):
            subBuffer (Variable):




