Tc2_MC2 (Library):
    enums (Namespace):
    statuses (Namespace):
    statemachines (StatemachinesNamespace):
        parts (Namespace):
        processes (Namespace):
        statuses (Namespace):
    configs (Namespace):
    structs (Namespace):
        ST_AxisStatus (Struct):
            UpdateTaskIndex (Variable):
            UpdateCycleTime (Variable):
            CycleCounter (Variable):
            NcCycleCounter (Variable):
            Error (Variable):
            ErrorId (Variable):
            Disabled (Variable):
            ControlLoopClosed (Variable):
            ExtSetPointGenEnabled (Variable):
        PLCTONC_AXIS_REF (Struct):
            ControlDWord (Variable):
            Override (Variable):
            AxisModeRequest (Variable):
            AxisModeDWord (Variable):
            AxisModeLReal (Variable):
            PositionCorrection (Variable):
            ExtSetPos (Variable):
            ExtSetVelo (Variable):
            ExtSetAcc (Variable):
            ExtSetDirection (Variable):
            ExtControllerOutput (Variable):
            GearRatio1 (Variable):
            GearRatio2 (Variable):
            GearRatio3 (Variable):
            GearRatio4 (Variable):
        NCTOPLC_AXIS_REF (Struct):
            StateDWord (Variable):
            ErrorCode (Variable):
            AxisState (Variable):
            AxisModeConfirmation (Variable):
            HomingState (Variable):
            CoupleState (Variable):
            SvbEntries (Variable):
            SafEntries (Variable):
            AxisId (Variable):
            OpModeDWord (Variable):
            ActiveControlLoopIndex (Variable):
            ControlLoopIndex (Variable):
            ActPos (Variable):
            ModuloActPos (Variable):
            ModuloActTurns (Variable):
            ActVelo (Variable):
            PosDiff (Variable):
            SetPos (Variable):
            SetVelo (Variable):
            SetAcc (Variable):
            TargetPos (Variable):
            ModuloSetPos (Variable):
            ModuloSetTurns (Variable):
            CmdNo (Variable):
            CmdState (Variable):
    processes (ProcessesNamespace):
        args (Namespace):
    functionblocks (Namespace):
        AXIS_REF (FunctionBlock):
    ST_AxisStatus (Struct):
        UpdateTaskIndex (Variable):
        UpdateCycleTime (Variable):
        CycleCounter (Variable):
        NcCycleCounter (Variable):
        Error (Variable):
        ErrorId (Variable):
        Disabled (Variable):
        ControlLoopClosed (Variable):
        ExtSetPointGenEnabled (Variable):
    PLCTONC_AXIS_REF (Struct):
        ControlDWord (Variable):
        Override (Variable):
        AxisModeRequest (Variable):
        AxisModeDWord (Variable):
        AxisModeLReal (Variable):
        PositionCorrection (Variable):
        ExtSetPos (Variable):
        ExtSetVelo (Variable):
        ExtSetAcc (Variable):
        ExtSetDirection (Variable):
        ExtControllerOutput (Variable):
        GearRatio1 (Variable):
        GearRatio2 (Variable):
        GearRatio3 (Variable):
        GearRatio4 (Variable):
    NCTOPLC_AXIS_REF (Struct):
        StateDWord (Variable):
        ErrorCode (Variable):
        AxisState (Variable):
        AxisModeConfirmation (Variable):
        HomingState (Variable):
        CoupleState (Variable):
        SvbEntries (Variable):
        SafEntries (Variable):
        AxisId (Variable):
        OpModeDWord (Variable):
        ActiveControlLoopIndex (Variable):
        ControlLoopIndex (Variable):
        ActPos (Variable):
        ModuloActPos (Variable):
        ModuloActTurns (Variable):
        ActVelo (Variable):
        PosDiff (Variable):
        SetPos (Variable):
        SetVelo (Variable):
        SetAcc (Variable):
        TargetPos (Variable):
        ModuloSetPos (Variable):
        ModuloSetTurns (Variable):
        CmdNo (Variable):
        CmdState (Variable):
    AXIS_REF (FunctionBlock):




